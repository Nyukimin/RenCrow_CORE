---
generated_at: "2026-07-15T17:26:00+09:00"
run_id: run_20260715_170940
phase: 2
step: "5-6"
profile: RenCrow_CORE_20260715_refined
artifact: file_analysis
module_group_id: docs_config_data
---

# docs_config_data ファイル解析

## 概要

docsは意味の正本、Config/prompt/schemaはruntime入力、rencrow-dataは独立した監査可能なPython pipelineである。

## ファイル: docs/02_正本仕様/00_正本仕様Tree.md

### 役割（Why）

current、versioned、To-Be、external bridgeの優先順位を決めるcanonical index。

### 公開 API

人間/agentが目的別読む経路とowner境界を参照する。

### 内部関数フロー

~~~mermaid
flowchart TD
  Question --> Tree --> SpecificSpec --> CodeAndTest --> RuntimeEvidence
~~~

### 落とし穴・注意点

- docs/refsは補助でありactive正本を上書きしない。
- worktreeには2026-07-15棚卸しの未コミット変更がある。

## ファイル: rencrow-data/src/rencrow_data/audit.py

### 役割（Why）

snapshot、fetch quality、decision、paper、weekly logを横断して監査reportを作る。

### 公開 API

build_audit_reportがread-only集計入口。

### 内部関数フロー

~~~mermaid
flowchart TD
  Snapshot --> HashCheck
  Snapshot --> FetchEvidence
  Decision --> RiskEvidence
  Paper --> ApprovalGate
  HashCheck --> Audit
  FetchEvidence --> Audit
  RiskEvidence --> Audit
  ApprovalGate --> Audit
~~~

### 落とし穴・注意点

- generated DB/reportはGit管理対象外。
- ViewerはDBのprojectionを表示し、投資判断の実行主体にならない。

## 関連ドキュメント

- ファイル一覧.md
- 関数一覧.md
- ../docs_config_data.md
