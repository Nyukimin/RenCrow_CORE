---
generated_at: "2026-07-15T17:26:00+09:00"
run_id: run_20260715_170940
phase: 2
step: "5-5"
profile: RenCrow_CORE_20260715_refined
artifact: file_analysis
module_group_id: infrastructure_persistence
---

# infrastructure_persistence ファイル解析

## 概要

ToolRunnerとconversation persistenceが横断的な安全・記憶境界である。providerはdomain/module DTOへ変換して外部技術を隔離する。

## ファイル: internal/infrastructure/tools/runner.go

### 役割（Why）

tool metadata、mediation、実runner、recordingを統一し、Chat/Workerへ異なるrunner集合を渡せるようにする。

### 公開 API

| API | 役割 |
| --- | --- |
| ExecuteV2 | typed responseを返す |
| ListTools | metadata discovery |
| WithWebGather* | optional provider注入 |

### 内部関数フロー

~~~mermaid
flowchart TD
  Call --> Lookup --> Mediate --> Execute --> Record --> Response
  Mediate -->|deny| Denied
~~~

### 落とし穴・注意点

- Chat runnerとWorker runnerのtool setを同一にしない。
- compatibility Executeはstringへ変換するためstructured errorを失わないよう注意。

## ファイル: internal/infrastructure/persistence/conversation/real_manager.go

### 役割（Why）

Redis/DuckDB/VectorDB/L1をconversation managerとして束ねるlegacy persistence facade。

### 公開 API

| API | 役割 |
| --- | --- |
| NewRealConversationManager | store群構築 |
| WithL1Store/Embedder/Summarizer | optional接続 |
| Close | resource解放 |

### 内部関数フロー

~~~mermaid
flowchart TD
  Conversation --> L1 --> Summarize --> Archive
  Archive --> DuckDB
  Knowledge --> VectorDB
  Close --> Stores
~~~

### 落とし穴・注意点

- Memoryのtruth sourceとprojection storeを区別する。
- close ownershipはcomposition rootと二重化しない。

## 関連ドキュメント

- ファイル一覧.md
- 関数一覧.md
- ../infrastructure_persistence.md
