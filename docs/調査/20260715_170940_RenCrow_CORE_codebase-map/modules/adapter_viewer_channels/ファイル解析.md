---
generated_at: "2026-07-15T17:22:00+09:00"
run_id: run_20260715_170940
phase: 2
step: "5-3"
profile: RenCrow_CORE_20260715_refined
artifact: file_analysis
module_group_id: adapter_viewer_channels
---

# adapter_viewer_channels ファイル解析

## 概要

Viewer入力はhandler_send、観測配信はEventHub、契約の純粋部分はmodulesで分離される。Viewer adapterにはなおfeature固有handlerが多い。

## ファイル: internal/adapter/viewer/handler_send.go

### 役割（Why）

JSON/multipart requestを統一SendRequestへ変換し、attachment保存後にapplication handlerへ渡す。

### 公開 API

| API | 役割 |
| --- | --- |
| HandleSend | attachmentなし互換 |
| HandleSendWithAttachments | production send |

### 内部関数フロー

~~~mermaid
flowchart TD
  HTTP --> ContentType
  ContentType --> ParseJSON
  ContentType --> ParseMultipart
  ParseMultipart --> SaveAttachment
  ParseJSON --> EffectiveMessage
  SaveAttachment --> EffectiveMessage
  EffectiveMessage --> Handler
  Handler --> Response
~~~

### 落とし穴・注意点

- toとroute prefixは別概念。
- empty messageはattachment時のみ補完される。

## ファイル: internal/adapter/viewer/hub.go

### 役割（Why）

event sequence、bounded history、SSE subscriberを同期付きで管理する。

### 公開 API

| API | 役割 |
| --- | --- |
| OnEvent | listener実装 |
| Subscribe/Unsubscribe | client lifecycle |
| History | copyを返す |

### 内部関数フロー

~~~mermaid
flowchart TD
  Event --> Lock --> Sequence --> History --> Unlock --> Marshal --> Broadcast
~~~

### 落とし穴・注意点

- slow clientへのsendはdropしてpublisherをblockしない。
- UnsubscribeとbroadcastはRWMutex順序でclose/send競合を避ける。

## 関連ドキュメント

- ファイル一覧.md
- 関数一覧.md
- ../adapter_viewer_channels.md
