---
generated_at: "2026-07-15T17:18:00+09:00"
run_id: run_20260715_170940
phase: 2
step: "5-1"
profile: RenCrow_CORE_20260715_refined
artifact: file_analysis
module_group_id: cmd_runtime
---

# cmd_runtime ファイル解析

## 概要

process境界はConfigを読み、legacy-bodyとfeature facadeを組み立てる。最重要変更点はmain、runtime_dependencies、routesの3点である。

## ファイル: cmd/rencrow/main.go

### 役割（Why）

唯一のHTTP process lifecycle owner。Config失敗は起動失敗にし、Tailscale guardをserver handlerへ適用する。

### 公開 API

| 関数 | 使用場面 |
| --- | --- |
| cmdRun | production server起動 |
| getConfigPath | RENCROW_CONFIGまたは既定pathの解決 |
| localOnlyHandler | pprofのloopback制限 |

### 内部関数フロー

~~~mermaid
flowchart TD
  A[cmdRun] --> B[LoadConfig]
  B --> C[buildDependencies]
  C --> D[registerFeatureRoutes]
  D --> E[ListenAndServe]
  F[signal] --> G[Dependencies.Shutdown]
  G --> H[os.Exit]
~~~

### 落とし穴・注意点

- gracefulというcommentに対しHTTP server.Shutdownは呼ばれず、in-flight requestはdrainされない。
- http.ServerにReadHeaderTimeout等がない。公開範囲はTailscale guard前提。

## ファイル: cmd/rencrow/runtime_dependencies.go

### 役割（Why）

domain/application/infrastructureをproduction handlerへ接続するcomposition root。

### 公開 API

| 関数 | 使用場面 |
| --- | --- |
| buildDependencies | process起動時に一度 |
| Dependencies.Shutdown | signal停止時 |

### 内部関数フロー

~~~mermaid
flowchart TD
  A[Config] --> B[providers and stores]
  B --> C[agents and orchestrator]
  C --> D[Viewer handlers]
  D --> E[background jobs]
  E --> F[Dependencies]
~~~

### 落とし穴・注意点

- 1,140行で、featureの追加・削除が広いdiffになりやすい。
- storeのClose所有がDependencies fieldごとに不均一で、追加時にShutdown登録を忘れやすい。

## 関連ドキュメント

- ファイル一覧.md
- 関数一覧.md
- ../cmd_runtime.md
