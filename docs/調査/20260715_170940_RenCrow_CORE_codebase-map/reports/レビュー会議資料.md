---
generated_at: "2026-07-15T17:25:00+09:00"
run_id: run_20260715_170940
phase: 3
step: "13"
profile: RenCrow_CORE_20260715_refined
artifact: report_review
---

# RenCrow_CORE レビュー会議資料

## 概要

- 対象: RenCrow_CORE
- 検出件数: 6件（高0、中4、低2）
- 構成: 深刻度「中」の4件を1件ずつ議論し、低2件は優先度表で扱う。

## [ERR-001] Signal shutdownがHTTP requestをdrainしない

**深刻度**: 中 | **モジュール**: cmd_runtime | **カテゴリ**: エラー処理

### 何が問題か

signal handlerが依存資源を閉じて直ちにprocess終了する。HTTP serverが保持する処理中requestとSSE接続へ終了猶予を与えていない。

### なぜ問題か

応答、永続化、完了eventの途中切断が状態不一致や重複再試行につながる可能性がある。

### 影響波及図

~~~mermaid
graph TD
  ROOT[ERR-001 signal] --> A[Immediate exit]
  A --> B[In-flight request cut]
  B --> C[Viewer or client state stale]
~~~

### あるべき姿 vs 現状

~~~mermaid
graph LR
  subgraph Expected
    E1[Stop accept] --> E2[Drain with deadline] --> E3[Close dependencies]
  end
  subgraph Actual
    A1[Close dependencies] --> A2[os.Exit]
  end
~~~

### 場所

`cmd/rencrow/main.go:89` — signal goroutine。`http.Server`生成は同fileの179行付近。

### 推奨対応

signalをmain control flowへ統合し、deadline付きserver shutdown後にbackgroundと依存資源を閉じるcontractをtestする。

### 議論ポイント

- SSE接続の最大drain時間を何秒にするか。
- background jobの停止順序を誰がownerとして持つか。

## [INCONSIST-001] Ops registrarが複数featureを所有する

**深刻度**: 中 | **モジュール**: feature_facades | **カテゴリ**: 不整合

### 何が問題か

revenue、advisor、agent profile、scheduler、workstreamのrouteがOps registrarへ集中し、対応featureの一部はno-opである。

### なぜ問題か

routeの責務とfile配置が一致せず、移行時の二重登録や変更影響の見落としが起きやすい。

### 影響波及図

~~~mermaid
graph TD
  ROOT[INCONSIST-001 Ops] --> A[Cross-feature ownership]
  A --> B[Large review surface]
  B --> C[Duplicate or missed route]
~~~

### あるべき姿 vs 現状

~~~mermaid
graph LR
  subgraph Expected
    E1[Revenue registrar]
    E2[Advisor registrar]
    E3[Workstream registrar]
  end
  subgraph Actual
    A1[Ops registrar] --> A2[Multiple route families]
  end
~~~

### 場所

`internal/features/ops/registrar.go` — feature横断route registration。

### 推奨対応

path/method contract testを固定し、一featureずつ旧登録削除と新登録追加を同じ変更で行う。

### 議論ポイント

- 最初に移すfeatureをどれにするか。
- Opsを恒久bounded contextとして残すrouteはあるか。

## [TEST-001] `cmd/rencrow-agent`にpackage testがない

**深刻度**: 中 | **モジュール**: cmd_runtime | **カテゴリ**: テスト不足

### 何が問題か

7 production fileから成るremote agent entryに同package testがない。

### なぜ問題か

flag、config、transport、exit behaviorの回帰が単体で切り分けられず、統合または運用まで検出が遅れる。

### 影響波及図

~~~mermaid
graph TD
  ROOT[TEST-001 no test] --> A[CLI change]
  A --> B[Regression escapes]
  B --> C[Remote operation fails]
~~~

### あるべき姿 vs 現状

~~~mermaid
graph LR
  subgraph Expected
    E1[Pure parse tests] --> E2[Fake transport tests] --> E3[Binary e2e]
  end
  subgraph Actual
    A1[Production package] --> A2[No package test]
  end
~~~

### 場所

`cmd/rencrow-agent` — 7 production Go files、0 `*_test.go`。

### 推奨対応

processを起動しないparse/config testから始め、network behaviorはfake server contractへ分離する。

### 議論ポイント

- remote protocolで最優先に固定すべきcontractは何か。
- binary e2eを通常CIへ入れるか、別jobにするか。

## [DEV-001] Current仕様が存在しない旧pathを指す

**深刻度**: 中 | **モジュール**: docs_config_data | **カテゴリ**: 仕様逸脱

### 何が問題か

current実装仕様が`pkg/agent/loop.go`と`pkg/agent/context.go`を参照するが、現在のtreeに存在しない。

### なぜ問題か

実装担当が正しいdomain/application/infrastructure境界へ到達できず、誤った変更計画を作る可能性がある。

### 影響波及図

~~~mermaid
graph TD
  ROOT[DEV-001 old path] --> A[Search fails]
  A --> B[Replacement guessed]
  B --> C[Wrong layer changed]
~~~

### あるべき姿 vs 現状

~~~mermaid
graph LR
  subgraph Expected
    E1[Current spec] --> E2[Existing implementation paths]
  end
  subgraph Actual
    A1[Current spec] --> A2[Missing pkg paths]
  end
~~~

### 場所

`docs/02_正本仕様/02_実装仕様/01_エージェント・責務境界.md:86`付近。

### 推奨対応

旧責務と現行pathの対応を確認し、current節を更新する。旧pathはsnapshot注記付き履歴へ移す。

### 議論ポイント

- 旧`pkg/agent`の各責務をどの現行packageへ対応付けるか。
- untracked文書分割を確定する前に修正するか、同じcommitへ含めるか。

## 対応優先度まとめ

| 優先度 | 件数 | 代表的な不備 |
| --- | ---: | --- |
| 即時対応 | 1 | ERR-001 |
| 計画対応 | 3 | INCONSIST-001、TEST-001、DEV-001 |
| 改善機会 | 2 | NI-001、DEV-002 |

## 関連ドキュメント

- 技術詳細: [エンジニア向けレポート.md](エンジニア向けレポート.md)
- 経営サマリ: [マネージャ向けレポート.md](マネージャ向けレポート.md)
