---
generated_at: "2026-07-15T17:23:00+09:00"
run_id: run_20260715_170940
phase: 3
step: "13"
profile: RenCrow_CORE_20260715_refined
artifact: report_engineer
---

# RenCrow_CORE エンジニア向けレポート

## 概要

全体build/testは正常で、公開moduleのdependency ruleも機械検証されている。一方、shutdown drain、feature route ownership、remote agent test、current正本のpath/versionに6件の改善点がある。高リスクはなく、既存機能を保った小さなcontract-first修正が適する。

## 検出結果サマリ

| カテゴリ | 件数 | 深刻度「高」 |
| --- | ---: | ---: |
| 仕様-実装乖離（DEV） | 2 | 0 |
| 未実装（NI） | 1 | 0 |
| 未要求実装（UNREQ） | 0 | 0 |
| エラー処理欠落（ERR） | 1 | 0 |
| 境界条件漏れ（BOUND） | 0 | 0 |
| 競合・タイミング（RACE） | 0 | 0 |
| 不整合（INCONSIST） | 1 | 0 |
| テスト不足（TEST） | 1 | 0 |

## モジュール別の不備詳細

### cmd_runtime

#### 即時対応が必要な項目

深刻度「高」は0件。

#### その他の項目

- ERR-001: `cmd/rencrow/main.go:89`以降のsignal handlerはdependency close後に`os.Exit(0)`し、`http.Server.Shutdown`を呼ばない。
- TEST-001: `cmd/rencrow-agent`は7 production fileに対しpackage testがない。

#### 修正時の影響範囲

- `cmd/rencrow/main.go`を変更した場合:
  - route起動、TLS/non-TLS、background job、SSE長寿命接続、store/provider close順序。
  - signal受信、timeout、in-flight request、二重shutdownをtestする。
- `cmd/rencrow-agent`を変更した場合:
  - flag/config parse、remote transport、audio routing、exit code。
  - pure parse testとfake transport contract testを分ける。

### feature_facades

#### 即時対応が必要な項目

深刻度「高」は0件。

#### その他の項目

- NI-001: 13 registrarがno-op予約境界のまま。
- INCONSIST-001: Ops registrarがrevenue、advisor、agent profile、scheduler、workstreamを横断登録する。

#### 修正時の影響範囲

- `internal/features/ops/registrar.go`を変更した場合:
  - `cmd/rencrow/routes.go`、各feature registrar、Viewer/CLI API path。
  - path/method/permissionのcontract testと二重登録の不存在を確認する。

### docs_config_data

#### 即時対応が必要な項目

深刻度「高」は0件。

#### その他の項目

- DEV-001: current実装仕様に存在しない`pkg/agent` pathがある。
- DEV-002: `CLAUDE.md`のGo 1.23と`go.mod`のGo 1.25.0が不一致。

#### 修正時の影響範囲

- 正本path更新時は旧責務をdomain/application/infrastructureへ分解した対応表を添える。
- versionは`go.mod`を最小version正本とし、local検証toolchainと混同しない。

### その他のモジュール

domain_application、adapter_viewer_channels、infrastructure_persistenceには今回単独の確定gapを置かなかった。ただしERR-001のshutdown順序とINCONSIST-001のroute移行で回帰確認対象になる。

## 推奨修正手順

1. Phase 1: graceful shutdownの期待contractとtestを先に追加し、server drainを実装する。
2. Phase 2: route path snapshotを固定し、Opsから一featureずつregistration ownershipを移す。
3. Phase 3: `cmd/rencrow-agent`のpure/fake transport testを追加する。
4. Phase 4: current正本の旧pathとGo version表記を修正する。

## 検証済みcommand

~~~bash
go list ./...
go test ./...
go vet ./...
make rencrow-data-test
~~~

いずれも成功した。`make rencrow-data-test`は107 tests、約78秒。`go test -race ./...`とlive/browser確認は今回未実施である。

## 関連ドキュメント

- [ギャップ分析.md](../ギャップ分析.md)
- [リスク分析.md](../リスク分析.md)
- [アーキテクチャ総合.md](../アーキテクチャ総合.md)
