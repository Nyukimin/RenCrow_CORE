---
generated_at: "2026-07-15T17:19:00+09:00"
run_id: run_20260715_170940
phase: 3
step: "10"
profile: RenCrow_CORE_20260715_refined
artifact: traceability_matrix
refs_scope: docs/02_正本仕様
---

# RenCrow_CORE トレーサビリティマトリクス

## 概要

`docs/02_正本仕様`を要求の正本として、実装・test・運用確認への順方向traceと、主要実装から要求への逆方向traceを確認した。行番号は2026-07-15の作業treeに対する調査時点である。

## 要件一覧

| ID | 正本要求 | 主な実装 | 検証 | 判定 |
| --- | --- | --- | --- | --- |
| REQ-001 | 明示recipientとcommandを優先してagent routeを決める | `internal/application/orchestrator/message_orchestrator.go` | orchestrator/routing tests、`go test ./...` | 実装済み |
| REQ-002 | Coderは提案、Workerは副作用実行を担う | `internal/domain/agent`、`internal/application/worker`、`internal/infrastructure/tools/runner.go` | worker/tool policy tests | 実装済み |
| REQ-003 | Runtime Configは検証し、必須値欠落を暗黙fallbackしない | `internal/application/runtimeconfig`、`cmd/rencrow/runtime_dependencies.go` | config tests、startup error path | 実装済み |
| REQ-004 | Viewerはrecipient選択、LLM readiness、実行状況を投影する | `internal/adapter/viewer`、`internal/features/llmops` | Viewer handler tests、live確認は別途必要 | 実装済み |
| REQ-005 | Memoryはrole可視性、budget、source traceを持つRecallPackとして注入する | `internal/domain/conversation/recall_pack.go`、`internal/application/memory` | conversation/memory tests | 実装済み |
| REQ-006 | TTSはtext、synthesis、audio fetch、playbackを区別する | `cmd/rencrow/tts_client_bridge.go`、`modules/tts` | bridge tests、browser playbackはlive確認が必要 | 実装済み |
| REQ-007 | Viewer SSEは再接続とtransient eventを扱う | `internal/adapter/viewer/hub.go`、`handler_sse.go` | hub/SSE tests | 実装済み |
| REQ-008 | Ver0.80の公開moduleとdependency方向を機械検証する | `modules/*`、`modules/dependency_rules_test.go` | `go test ./...` | 実装済み |
| REQ-009 | feature単位のregistrar/facadeへroute ownershipを移す | `internal/features/*/registrar.go`、`cmd/rencrow/routes.go` | architecture tests | 部分実装 |
| REQ-010 | Game BridgeはGAMESとruntimeの責務を分離し、bounded contractを使う | `modules/gamebridge`、`internal/adapter/gamebridge` | gamebridge tests | 実装済み |
| REQ-011 | Advisorはapproval policyを通し、Codex結果をadvisoryとして扱う | `modules/advisor`、`internal/application/advisor`、`internal/infrastructure/advisor` | advisor tests | 実装済み |
| REQ-012 | Economic loopはcandidateをdraft-onlyで作り、人間判断を越えない | `modules/economic`、`internal/application/economic`、`internal/application/heartbeat` | economic/heartbeat tests | 実装済み |
| REQ-013 | STT/TTS/LLM/Vision/Toolsは外部module境界を保つ | `modules/stt`、`modules/tts`、`modules/llm`、`modules/vision`、`modules/tools` | module contract/dependency tests | 実装済み |
| REQ-014 | Config・prompt・skill・schema・運用dataをrepo内で追跡可能にする | `config`、`prompts`、`skills`、`schemas`、`rencrow-data` | `make rencrow-data-test`、schema tests | 実装済み |

## 逆方向追跡（仕様にない実装）

~~~mermaid
graph LR
  CMD[cmd runtime] --> R3[REQ-003]
  CMD --> R9[REQ-009]
  ORCH[orchestrator] --> R1[REQ-001]
  WORKER[Worker and ToolRunner] --> R2[REQ-002]
  VIEWER[Viewer HTTP and SSE] --> R4[REQ-004]
  VIEWER --> R6[REQ-006]
  VIEWER --> R7[REQ-007]
  MEMORY[Recall and stores] --> R5[REQ-005]
  MODULES[public modules] --> R8[REQ-008]
  MODULES --> R13[REQ-013]
  ADVISOR[Advisor] --> R11[REQ-011]
  ECON[Economic] --> R12[REQ-012]
  GAME[Game Bridge] --> R10[REQ-010]
  ASSETS[Config docs data] --> R14[REQ-014]
~~~

| 実装領域 | 対応要求 | 未追跡の可能性 |
| --- | --- | --- |
| `cmd/rencrow` composition root | REQ-003、REQ-009 | signal時HTTP drainは正本に明示要求がなく、実装riskとして別記 |
| `internal/application` | REQ-001、REQ-002、REQ-005、REQ-011、REQ-012 | 確認した主要serviceに正本なし実装は未確認 |
| `internal/adapter/viewer` | REQ-004、REQ-006、REQ-007 | Viewer細部は機能台帳とhandler testsへ分散 |
| `internal/features` | REQ-009 | no-op registrarが残り、ownership移行が未完 |
| `internal/infrastructure` | REQ-002、REQ-005、REQ-011、REQ-013 | provider固有設定はConfig docsにもtraceが必要 |
| `rencrow-data` | REQ-014 | `docs/株式`側に詳細仕様があり、今回のrefs範囲外 |

## ギャップサマリ

| 項目 | 件数 |
| --- | ---: |
| 正本要求 | 14 |
| 実装済み | 13 |
| 部分実装 | 1 |
| 未実装 | 0 |
| 主なtestまたは機械検証あり | 14 |
| live/browser確認を追加すべき要求 | 2 |

順方向coverageは100%（14/14に実装参照あり）、完了coverageは92.9%（13/14）。これは要求の存在確認であり、すべてのedge caseや本番live挙動を保証する値ではない。

~~~mermaid
flowchart LR
  REQ[14 canonical requirements] --> DONE[13 implemented]
  REQ --> PART[1 partial REQ-009]
  PART --> OPS[Ops centralized routes]
  PART --> NOOP[13 reserved no-op registrars]
~~~

## 関連ドキュメント

- `refs_mapping.md`
- `ギャップ分析.md`
- `履歴チェック.md`
- `docs/02_正本仕様/12_CORE_機能台帳.md`
