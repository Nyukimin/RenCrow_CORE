---
title: 調査 — ChatGPT export L3 が減少し UserMemory 想起まで到達しない
date: 2026-08-15 21:38
status: resolved_with_backfill
skill: debug-investigate
symptom: ChatGPT_20260713 の 54,032 message import 後に L3 が 25,032 件まで減少し、UserMemory 投影 job も大量に timeout していた
frequency: lifecycle 起動時と ProfilePromotion 実行時
inputs: SQLite read-only 集計、systemd journal、prompt-debug、ChatGPT export manifest
related: docs/02_機能仕様.md, docs/04_アーキテクチャ概要.md
---

## 概要

ChatGPT export の欠落原因は、L1 raw 用 lifecycle compact が `layer` を制限せず、古い `conv:*` の L3 export まで削除していたことだった。L1 のみを compact するよう修正し、54,032 件を再投入した。レビュー済み normal UserMemory 11 件を confirmed にし、4 Agent の実 Chat で prompt 注入と想起応答を確認した。

---

## 調査経緯

### 仮説1: lifecycle compact が import 済み L3 を削除している

- **根拠**: manifest は 54,032 件、import event も全件成功だが、live DB は 25,032 件だった。起動 log に `raw_compacted=1000` が繰り返し記録されていた。
- **検証結果**: 確認
- **証拠**:
  - 旧 SQL は `namespace LIKE 'conv:%'` と `memory_state=observed` のみで、`layer='L1'` 制約がなかった。
  - `AND layer = MemoryLayerL1` 追加後の回帰 test で、古い L1 は削除され、同じ古さの ChatGPT L3 は残った。
  - 修正 binary の lifecycle 結果は `raw_compacted=0`。再投入後の再起動で 54,032 件が維持され、`PRAGMA quick_check=ok`。
- **チェックリスト結果**:
  - ☑ 確証バイアス: import 失敗と対比し、manifest、import 履歴、DB 件数が初回 import 成功を支持することを確認した。
  - ☑ 頻度制約: 1,000 件ずつの減少と compact limit が一致した。
  - ☑ ライフサイクル: import → restart/maintenance →減少の順と修正後の再起動を確認した。
  - ☑ 既存知見: L3 を immutable provenance とする CORE 仕様と整合する修正である。

### 仮説2: ProfilePromotion の 45 秒 timeout が Worker の完了より先に到達する

- **根拠**: ChatGPT 由来 failed 1,067 件のうち1,062 件が `context deadline exceeded`。Gateway の処理時間は約44〜45秒だった。
- **検証結果**: 確認（新規 job の継続消化は残課題）
- **証拠**:
  - live config の `profile_promotion_timeout_seconds` を45秒から180秒へ変更した。
  - job 現状は completed 555 / failed 1,222 / pending 17,704。ChatGPT 由来は completed 448 / failed 1,067 / pending 17,694。
  - foreground Chat/IdleChat が Worker を使うと実行中 promotion が `context canceled` になる場合がある。今回の即時想起は、既存のレビュー済み candidate を正規 state API で confirmed 化して成立させた。
- **チェックリスト結果**:
  - ☑ 確証バイアス: JSON schema error 5 件は timeout と分離した。
  - ☑ 頻度制約: failed の大多数が deadline である集計と一致する。
  - ☑ ライフサイクル: queue、lease、retry、foreground cancellation を別々に確認した。
  - ☑ 既存知見: candidate の自動 confirmed 化を禁じる仕様は維持した。

### 仮説3: confirmed UserMemory は実 Agent prompt に入っていない

- **根拠**: `recall_trace_item` に UserMemory 行が現れなかった。
- **検証結果**: 棄却（trace の観測欠落は確認）
- **証拠**:
  - prompt-debug で Mio/Shiro/Kuro/Midori それぞれの実 request に `prompt_context_type=recall_pack`, `recall_section=user_profile` の 560 文字 block があった。
  - 4人の block SHA-256 は全て `ad50a1d58ac8ffd665eaa1d89d64d50fb75f6609250c78721bf3c93bf962af9f` で、対象の想起記憶を含んでいた。
  - 4人とも正規 `RenCrow_CMD -> CORE Viewer -> CHAT -> Agent -> LLM` 経路で、関連場面で連想的に想起・活用する旨を回答した。
  - `RecallPack.ToTraceItems()` は L0/L2/L3/Knowledge 系を記録するが `UserProfile.Facts` を trace item へ変換しない。
- **チェックリスト結果**:
  - ☑ 確証バイアス: Agent の回答文だけでは合格にせず、実際の prompt block を確認した。
  - ☑ 頻度制約: 4 Agent すべてで同じ block と回答を確認した。
  - ☑ ライフサイクル: candidate は非注入、confirmed normal は注入、private candidate は非注入とした。
  - ☑ 既存知見: UserMemory は Knowledge Memory ではなく RecallPack UserProfile へ入るという正本責務と一致した。

### 仮説4: ProfilePromotion の idle lease が IdleChat のbusy判定から見えない

- **根拠**: timeoutを180秒へ延長した後も、IdleChat等のbackground生成と重なったProfileExtractorで`context canceled`が継続した。
- **検証結果**: 確認、修正済み
- **証拠**:
  - 旧`llmBusyTracker.Snapshot()`は実LLM sourceだけを集計し、取得中のidle leaseを`ExternalBusy`へ含めていなかった。
  - IdleChatは`ExternalBusy`確認後にproviderを開始するため、確認と開始の間にProfilePromotionがleaseを取得する競合窓もあった。
  - Red testはlease保持中の`ExternalBusy=false`と、IdleChat requestがinner providerまで到達する失敗を再現した。
  - 修正後はleaseを`memory_profile_promotion` sourceとしてbusy snapshotへ投影し、tracked provider直前の原子的admissionでもIdleChatをbackendへ渡さない。
  - live statusで`memory_profile_promotion=1`、`external_llm_busy=true`を確認し、ProfilePromotionは13:13:16に7 message／17 candidateを完了した。
  - foreground Chat job `20260815-131334-9579283b` は13:13:35に実行中ProfileExtractorをcancelし、13:14:54にMio応答を完了した。延期された3 jobはattempt 0のまま再claimされ、13:15:51に3 message／10 candidateを完了した。
- **チェックリスト結果**:
  - ☑ 確証バイアス: busy表示だけで合格にせず、provider非到達test、実job完了、foreground cancel、attempt非消費、再完了を分離して確認した。
  - ☑ 頻度制約: timeout失敗、IdleChat競合、foreground preemptionを別原因として扱った。
  - ☑ ライフサイクル: idle取得、実行、background抑止、foreground延期、再claim、完了までを確認した。
  - ☑ 既存知見: foregroundを常に優先し、candidateだけを生成する既存安全契約を変更していない。

---

## 根本原因

- **原因1**: L1 raw compact SQL の `layer` 制約漏れ。
- **メカニズム1**: `conv:* + observed + old` に一致する ChatGPT L3 を L1 raw と誤認し、1回に最大1,000件削除した。
- **原因2**: ProfilePromotion の CORE timeout 45秒が実 Worker latency とほぼ同じで、完了余裕がなかった。
- **原因3**: idle leaseが共通busy snapshotへ投影されず、IdleChatのscheduler確認とprovider開始も原子的でなかった。
- **影響範囲**: ChatGPT L3 episode の完全性、プロファイル投影率、UserMemory の想起可能な範囲。

## 修正内容

1. lifecycle compact 対象を `layer='L1'` に限定し、L3 保存の回帰 test を追加した。
2. 修正 binary を build/install し、CORE を再起動した。旧 binary と DB の consistent backup を保存した。
3. ChatGPT export artifact 54,032 件を idempotent 再投入した。
4. live ProfilePromotion timeout を180秒に延長した。
5. 証拠済み normal candidate 11 件を `/viewer/memory/user/state` で confirmed 化した。private 3 件は candidate のまま維持した。
6. 4 Agent の実 Chat と prompt-debug で UserProfile 注入・想起を検証した。
7. idle leaseを名前付きbusy sourceとして共通snapshotへ投影し、lease保持中のIdleChatをtracked provider直前でも原子的に拒否した。foreground preemptionは維持した。
8. CORE commit `1e79578d6fb143e864ead9fd385271be5804a120`をbuild/installし、停止境界を確認して再起動した。

## 検証結果

- focused lifecycle test: pass
- `go test ./internal/infrastructure/persistence/conversation/l1sqlite`: pass
- `go test ./internal/application/memorypromotion`: pass
- `go test ./internal/adapter/viewer`: pass
- `go test ./cmd/rencrow`: pass
- relevant `go vet`: pass
- `go build ./...`: pass
- `git diff --check`: pass
- live ready: `status=ready`
- live DB: ChatGPT L3 54,032件、user 19,209件、assistant由来 34,823件、`quick_check=ok`
- live UserMemory: reviewed normal confirmed 11件、reviewed private candidate 3件
- 4 Agent E2E: Mio/Shiro/Kuro/Midori で prompt UserProfile 注入と想起応答を確認
- TDD Red: lease保持中のbusy非表示、IdleChat inner provider到達を再現
- TDD Green: `go test ./cmd/rencrow`、MemoryPromotion、IdleChat、明示source root全体、`test/e2e`がpass
- live idle lease: `sources.memory_profile_promotion=1`、`external_llm_busy=true`、IdleChat `chat_active=false`
- live ProfilePromotion: 7 message／17 candidate完了、foreground cancel後にattempt 0で再開し3 message／10 candidate完了
- live foreground: Mio CHAT job `20260815-131334-9579283b`がpromotionをpreemptして応答完了
- deployed binary SHA-256: `dec4e2437b2dfe9b75a577a9977ca272cef7f1478e21394d4551c10bf8bb564b`
- restart後: ready、L3 54,032件、SQLite `quick_check=ok`

## 残課題

- 2026-08-15 13:15 UTC時点のChatGPT由来ProfilePromotion jobはcompleted 507 / failed 1,067 / pending 17,611 / running 24。全会話から追加UserMemory candidateを生成するbackground backfillは未完了。
- failed job を正規に reset/retry する運用 API がない。直接 DB 書き換えは行っていない。
- ChatGPT由来の残りpending jobはbackground backfillとして継続中。競合修正後のproduction E2E時点でcompleted 614 / failed 1,222 / pending 17,622 / running 24。
- visible Chat surfaceとの排他により、liveの手動IdleChat開始は409で拒否された。表示状態は変更せず、live busy snapshotとstartup background抑止、provider非到達testをIdleChat側証拠とした。
- Recall trace に UserProfile/UserMemory の memory ID が出ないため、現状の厳密な注入証拠は prompt-debug に依存する。

## 関連ソースファイル

- `internal/infrastructure/persistence/conversation/l1sqlite/l1_sqlite_lifecycle.go:145` - L1 raw compact 対象の layer 制約
- `internal/infrastructure/persistence/conversation/l1sqlite/l1_sqlite_lifecycle_test.go:144` - ChatGPT L3 保全回帰 test
- `internal/infrastructure/persistence/conversation/engine_impl.go:291` - confirmed/pinned normal UserMemory のランクと UserProfile 投影
- `internal/domain/conversation/recall_pack.go:135` - UserProfile の prompt 注入
- `cmd/rencrow/llm_busy_tracker.go` - idle leaseのbusy投影、IdleChatの原子的admission、foreground preemption
- `cmd/rencrow/runtime_memory_promotion.go` - `memory_profile_promotion` lease source
- live deployment `core.yaml` のconversation節 - production timeout config

## 教訓（将来の調査への知見）

- import 成功と現在件数は別に検証し、startup lifecycle を含む時系列で確認する。
- Agent が正解しただけで想起成功にせず、prompt 境界の注入証拠を確認する。
- immutable L3、review candidate、confirmed UserMemory、prompt injection を別の受け入れ条件とする。
- Serena MCP はこの実行環境で利用できなかったため、再利用可能な証拠は本調査記録に残した。
