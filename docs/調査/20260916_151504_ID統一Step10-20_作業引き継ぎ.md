# Canonical ID 再構築 Step 10-20 作業引き継ぎ

- 記録時刻: 2026-09-16 15:15 JST
- 作成理由: 途中状態のまま次セッションへ渡す
- 正本Goal: RenCrow_CORE の Canonical ID 再構築。Step 10-20 を依存順に、受入条件固定 → TDD → 既存 owner/認証/policy/正規 runtime route の本番同等 E2E → 再起動後検証 → 論理的な日本語 commit → Push。Step 20 で旧ID・互換層・dual read/write・旧生成器・旧Viewer query を除去。Golden / Restart / Recovery / 長時間・Background Task / Voice / Atlas / Memory / Viewer / ID integrity scan が揃うまで完遂。
- 禁止: 他作業差分、LLM Ops、根拠なき削除、port 変更、backend 直結、代替 model/偽 server。途中確認なしで進める（GPU 一時切替だけユーザー承認済み）。

## いまの到達点（1行）

Step 10 / 11 完了。Step 12 は source と LLM/STT/Vision/TTS/restart まで完了。TTS は 205 の Irodori-TTS-v4.1-Anime を正規経路に載せ、CORE Action/Attempt/Request/Response receipt を browser-only の orchestrator session 経路で取得済み。Step 13 は source（`8a867f1`）・移行・architecture zero・cutover receipt 済みで、2026-09-21 に BrowserTrace `APIArtifact` の typed `ArtifactID` + `ArtifactKind` を source 側へ実装し終え（Astra の独立 5 package 実行が exit 0）、check plan は `partial`（revenue の本番生成実績 0、`api_artifact` は 2026-09-21 12:41 UTC の read-only inventory で live 6 rows・`artifact_kind` 保持 0・全件が旧 opaque ID と実測（run orphan 0、browsertrace 由来 JSONL なし、実測ログ `Tmp/test-runtime/_qwen-supervision/s13_api_artifact_inventory.log` PROBE 11-16b）。remint・配備・再起動実測も未実施）。Step 14 は EvidenceID 派生 0・orphan 0（意味/実測）を store API `CountComplexityIdentityOrphans` と owner tests で機械強制し、live の空 FK index 列 8 行を backfill、binary 配備と正規 POST 201・再起動後維持まで確認済みで check plan は `partial`（現行 binary は HEAD `082ad99` の `5d010023d1…`）。Step 15-20 未着手。

## Owner / 操作環境

| 項目 | 値 |
| --- | --- |
| 作業 owner | RenCrow_CORE（TTS echo 契約だけ RenCrow_TTS） |
| CORE branch | `identity/03-dci`（ユーザー指示 2026-09-21: Step 20 完了まで現状維持。main→Identity／Identity→main のマージ、cherry-pick、rebase、branch 切替・新 branch 作成なし。HEAD 配備に必要な main 側 7 commit の統合単位 U1-U4 は Step 20 前の必須作業に追加せず、配備版との差分は観測事実として check plan の `outcome.open` に保持） |
| TTS branch | `main` |
| CORE 最終関連実装 | `447485d` 再起動回復の一括 List、`d70c4d6` context_pack run_id 移行、`ad93eaa` browser-only で entry 自律合成を計画しない（配備対象） |
| TTS 関連 commit | `70c1220` CORE の RequestID を TTS 応答で echo |
| 配備 binary | `~/.local/bin/rencrow` sha256 `f79071c1059401b69f86898ce83158b0c7049d8a00ee1512eeef049917e53872`（2026-09-21 12:21 UTC 実測、embedded `vcs.revision=351def7`、`vcs.modified=false`。HEAD `f159ed9` の配備ではない。履歴: 2026-09-17 23:38 JST 配備は sha256 `5d010023d107eb0a…`＝HEAD `082ad99`） |
| CORE listen | `0.0.0.0:18790` 固定。`/health/ready` |
| LLM Gateway | `127.0.0.1:8090` |
| TTS Gateway | `http://192.168.1.205:7870`（Windows Win11-HP01） |
| Irodori | `127.0.0.1:7880` on 205。正本モデルは `Irodori-TTS-v4.1-Anime`（`IRODORI_CHECKPOINT=phasefield-audio/Irodori-TTS-v4.1-Anime/int4-weight-only`）。CORE からは Gateway 経由のみ。`/health/ready` に model 名が出る（`/health` は 404） |
| Gemma4 | Windows Scheduled Task `RenCrow-Gemma4-RTX5060Ti`、`:18084` |
| GPU | RTX 5060 Ti。Gemma4 と Irodori は同時不可 |
| SSH 先 Windows | `ssh -i ~/.ssh/rencrow_agent nyuki@192.168.1.205` |
| resilience | `rencrow-resilience.timer` OnUnitActiveSec=2min、`AUTO_REPAIR=true` CODE2。listen 前に止める |

## 完了している Step

### Step 10 Task/Run ID

閉じ済み。TDD / E2E / Push 済み。作業を混ぜない。

### Step 11 ActionID / AttemptID

閉じ済み。TDD / E2E / Push 済み。

### Step 12 RequestID / ResponseID（完了）

受入の正本: `docs/architecture/identity/IDENTITY_CANONICAL.md` の Step 12。check plan: `docs/調査/20260916_042228_ID統一Step12_RequestResponse_check_plan.json`。

閉じたもの:

- TransportManager JSONL。1 send = 1 canonical `req_*` UUIDv7。ResponseID `rsp_*` を link。
- provider ID は ExternalRef / `target_request_id` のみ。
- Action/Attempt lineage は Step 11 を維持。
- CORE TTS は `X-RenCrow-TTS-Request-Id` を送り、echo 不一致は FailWithoutResponse。
- LLM / STT / Vision の実 Gateway E2E と controlled restart。
- TTS Gateway `projectCallerRequestID`: header があれば `request_id` を caller にし、target の `tts_*` を `target_request_id` へ。
- 再起動回復の件数分 JSONL 再読を止めた: `447485d`。
- TTS の CORE Action/Attempt/Request/Response receipt: browser-only では合成を orchestrator session（`TTSBridge`）へ一本化し、正規 `/entry` route で `req_*`/`rsp_*`/`external_ref=tts_*` を取得。details は `docs/調査/20260916_091600_ID統一Step12_listen復旧とTTS_echo.md` と check plan の `replacement_receipts`。
- `/entry` の local player 依存: `6163aa3` で任意化、`ad93eaa` で browser-only では `tts-synthesize`/`tts-playback` を計画しない（player 無しで error ではなく合成 step を立てない）。
- 同一要求での二重合成（session チャンク合成 + entry の全文合成）: entry 側の全文合成は browser 配信経路（`tts.audio_chunk` event）に繋がらない生成物のみだったため entry 側を廃止。実測で `workspace/tts/` の wav 増分 0 件を確認。
- Viewer chat の `context_pack.run_id NOT NULL`: `d70c4d6` が旧スキーマを移行。live DB `superagent_harness.db` で `pragma_table_info` の `run_id notnull=0` を確認。

未完了（Step 12 の範囲外）:

- Gemma4 と Irodori の GPU 同時常駐は未対応（現行は手動切替。常駐化は Step 13 以降の判断）。
- `TTS route update degraded: parallel limit exceeded` は `global_concurrency: 2` の既存上限で、合成 session が立つ前に抜ける既存 degraded 挙動。恒常障害ではないが改善候補。

## 重要な実装位置

| 責務 | path |
| --- | --- |
| CORE 起動依存 | `RenCrow_CORE/cmd/rencrow/runtime_dependencies.go`。HTTP listen は return 後 `main.go` |
| Action 再起動回復 | `cmd/rencrow/runtime_action_recovery.go` |
| SuperAgent 再起動回復 | `internal/application/superagent/run_recovery.go` |
| Task JSONL 再読 | `internal/infrastructure/persistence/task/transaction_snapshot.go` |
| TTS retry / identity | `internal/infrastructure/tts/rencrow_tts_retry.go` |
| Entry TTS | `cmd/rencrow/autonomous_entry.go` `isTTSRequest` / `configured()` |
| TTS Gateway echo | `RenCrow_TTS/gateway/internal/gateway/server.go` `projectCallerRequestID` |
| Gemma 停止正本 | `E:\GenerativeAI\RenCrow\RenCrow_Gemma4\ops\stop.ps1` |

## 運用で踏んだ罠

1. SIGQUIT は dump 後に process が死ぬ。listen 待ち中に使うな。
2. resilience は listen 前に CORE を約6分周期で落とす。再起動前は `systemctl --user stop rencrow-resilience.timer`。ready 後に start。
3. ChatGPT import 約140s。復旧後 startup_total 約192s。
4. 新 TTS Gateway は Irodori warmup fail-closed。
5. Windows SSH は `ssh 192.168.1.205 powershell.exe ... -EncodedCommand`（UTF-16LE base64）。
6. 正規 TTS は orchestrator `TTSBridge`。`/entry` の player 必須は本番と不一致。

## GPU 一時切替（承認済み手順）

1. `Stop-ScheduledTask RenCrow-Gemma4-RTX5060Ti`
2. `pwsh -ExecutionPolicy Bypass -File E:\GenerativeAI\RenCrow\RenCrow_Gemma4\ops\stop.ps1`
3. Irodori Enable/Start、`:7880/health/ready` 待ち
4. `Start-ScheduledTask RenCrow-TTSGateway`
5. CORE TTS E2E
6. Gateway / Irodori 停止、Irodori Disable、Gemma4 Start、`http://127.0.0.1:18084/v1/models` 200

## 次にやること

1. ~~Step 12 TTS の CORE receipt~~ 完了。browser-only では orchestrator session に合成を集約し（`ad93eaa`）、正規 `/entry` route で receipt を取得。
2. ~~GPU 切替 → 実合成 → JSONL~~ 完了。`req_*` / `rsp_*` / `external_ref=tts_*` を lineage 付きで確認。
3. ~~check plan の `s12_tts_transport` を pass~~ 完了。`outcome.passed` に反映。
4. Gemma4 を戻す（ユーザー判断。TTS は Anime 側で稼働継続可）。
5. ~~Step 13~~ 一部完了（`docs/調査/20260907_ID統一Step13_Artifact_check_plan.json` を `post-deploy-route-verified` / `partial` に更新）。2026-09-21 に `api_artifact` の typed ArtifactID 化と `artifact_kind` 付与は source 側（domain 単一宣言 + 追加 test 7 件、Astra 独立 5 package 実行 exit 0）で完了。残件: revenue ArtifactID projection の実生成証跡、`api_artifact` live row の remint 移行／対応 binary 配備／再起動後 route 実測（2026-09-21 12:41 UTC の read-only inventory で live 6 rows・`artifact_kind` 0・全件旧 opaque ID を実測、実測ログ `Tmp/test-runtime/_qwen-supervision/s13_api_artifact_inventory.log` PROBE 11-16b）、Supersede / Content hash は browsertrace APIArtifact に source 実装済（2026-09-21 13:38:45-13:39:19 UTC に Astra 独立実行 6 package exit 0、`Tmp/test-runtime/_qwen-supervision/astra_hash_source_independent.json`。形式の単一宣言 `modules/core/artifact_content.go`、生成・field 使用は `internal/domain/browsertrace`）。`APICoverageReport` への content hash／supersession（IU-18c1）も source 実装まで完了（本文投影の単一宣言 `internal/domain/browsertrace/coverage_content.go`＝4 list 固定順・nil と空 list は同一 digest、identity・created_at・hash 自身・後継参照は非混入。Qwen 局所検査の赤→緑 exit 0 と、Astra が編集停止後に同一 HEAD `f159ed9` で再実行した full package／Windows vet／build exit 0 を区別して保持、`Tmp/test-runtime/_qwen-supervision/astra_coverage_stable.json` 14:45:19-14:46:10 UTC。worker 側は socket 制約で application／viewer full が FAIL のため 6 package 完走証跡は Astra のみ）。`ContextPack` への content hash／supersession（IU-18c2）も source 実装まで完了（本文投影の単一宣言 `internal/domain/superagent/contextpack_content.go`＝`summary`→`included_sources`→`token_estimate` 固定順・nil と空 list は同一 digest、identity・created_at・hash 自身・後継参照は非混入、`token_estimate` は保存済みコンテキストの見積りなので本文。producer は `internal/application/orchestrator/superagent_runtime.go` の保存直前で stamp、client は DTO 全体を domain へ struct 変換して本文 field を再列挙しない。Qwen 局所検査＝赤 `s13_cp_domain_red.log` exit 1 の 4 失敗→緑 domain／persistence full・client 契約 focused・producer focused exit 0、Astra 独立受入＝`astra_cp_source.json` と各.log 2026-09-21 15:30:05-15:31:08 UTC で 5 package full・Windows/amd64 CGO 無効の vet／build exit 0・`source_changed_during_test` なし。live `context_pack` row への `content_hash` 付与と route 実測は未実施）。`DailyRoutineReport`／`ChannelDraft` への content hash／supersession（IU-18c3）も source 実装まで完了（本文投影の単一宣言 `internal/domain/revenue/artifact_content.go`＝report 10 field・draft は channel→subject→body 固定順・nil と空 list は同一 digest、`status`／`external_send_applied` は policy 状態なので非混入。producer は `BuildDailyRoutineReport`、viewer create は hash 未指定時だけ stamp し不正な非空入力は store 拒否へ返す。Qwen 局所緑＝store full・client 契約 focused・vet/gofmt exit 0（client full は socket 制約で未完走）、Astra 独立受入＝`astra_revenue_stable.json` と各.log 2026-09-21 16:54:50-16:55:22 UTC で persistence／client full・Windows vet／build exit 0・`source_changed_during_test` なし。live revenue row への `content_hash` 付与と route 実測は未実施）。`VerificationReport` への content hash／supersession（IU-18c4）も source 実装まで完了（本文投影の単一宣言 `internal/domain/verification/artifact_content.go`＝固定宣言順・nil と空 list は同一 digest・nested `Claim.Evidence` も正規化、identity metadata と Save 失敗注釈は非混入。producer は `internal/application/verification/pipeline.go` が確定本文から stamp し摘要失敗時は Save を呼ばず error を返す。保存は `internal/infrastructure/persistence/verification/report_store.go` の JSONLReportStore、公開は `internal/adapter/viewer/verification_handler.go` の domain 直接 JSON encoder で `rencrowclient` DTO 新設なし。domain 単位は Astra 独立 exit 0＝`astra_verification_domain.json` 17:34:19-17:34:20 UTC、source 接続は Astra 独立 5 package full／Windows vet／build exit 0＝`astra_verification_source.json` 17:50:36-17:51:27 UTC。Qwen 局所は赤→緑と focused 緑のみ実測で socket 制約により full 未完走。live verification report row への `content_hash` 付与と route 実測は未実施）。IU-18d の Supersede 操作（`SupersedeAPIArtifact`）と通常 Save の迂回封じ込めは browsertrace SQLite の source 実装まで完了（stage1-4、2026-09-21 20:15-21:18 UTC。Qwen 実測=persistence／domain browsertrace full・vet・build exit 0、`Tmp/test-runtime/_qwen-supervision/s13_supersede_stage4*.log`。Astra独立受入は2026-09-21 21:27 UTCに完了。6package full／Windows vet・build／focused -raceが全exit0、source hash差分0、証跡 `Tmp/test-runtime/_qwen-supervision/astra_sqlite_supersede_source.json`。SQLite source単位だけの受入で、以降の未適用項目は維持）。JSONL APIArtifact の WAL foundation（constructor が error を返し jsonlbatch.New→Recover→lock下load＋validate、Saveはbatch.Write、Listはlock内private loadで最終出現のみ新着優先、16MiB境界付きreader、64KiB超受理）は2026-09-21 22:15 UTCにQwenがsource実装し、focused test 3件 exit 0・persistence/browsertrace full exit 1は既知の意図的Redのみ・vet/build exit 0（`s13_jsonl_focused_red.log`／`s13_jsonl_store_full.log`／`s13_jsonl_vet.log`）。その後、通常Save guard・全writer同一lock下のSupersede・共有chain・並行/cancel/WAL recoveryまでQwenが実装し、2026-09-22 00:13:45–00:15:28 UTCにAstra独立5check全exit0・source hash差分0でsource受入（`Tmp/test-runtime/_qwen-supervision/astra_jsonl_supersede_source.json`）。作成事実の正準 Event 公開は 2026-09-22 に Qwen が durable 公開意図を source 実装した（domain 単一宣言 `internal/domain/browsertrace/publication.go`＝`artifact.created`・component `browsertrace`・`ActorKind=agent`・payload 3 key。作成時検査と、保存済み intent 専用の不変参照検査を分離して後続の本文更新・後継追加を起動失敗化しない。両 store 共通 `CreateAPIArtifactWithPublicationIntent`／`ListAPIArtifactPublicationIntents`＝SQLite は `api_artifact_publication_intent` を既存 BEGIN IMMEDIATE tx 内、JSONL は `api_artifact_publication.jsonl` を同一 jsonlbatch WAL に登録、List は `artifact_id` 排他 keyset・page 既定 50／上限 1000 clamp・総量上限なし。`internal/application/browsertrace/publication.go` が正準 v7 EventID 採番（`EventSeq=0`）と canonical `GetByID`→`AppendSequenced`→readback 比較・keyset drain を所有。`cmd/rencrow/runtime_browser_trace_recovery.go` を `runtime_dependencies.go:1231`（両 store open 後・route 起動前・30s・失敗は WARN）から呼び、fetcher proposal handler は保存と同時に作成事実を公開）。Qwen 局所実測＝focused fetcher 5 test exit 0・domain／persistence／jsonlbatch full exit 0・build／vet／gofmt exit 0（`s13_fetcher_proposal_creation_fact.log`／`s13_step13_src_packages.log`／`s13_fetcher_proposal_vet_gofmt.log`）。sandbox の loopback listen が `socket: operation not permitted` のため viewer／application full は socket 必須 test を `-skip` した完走（exit 0＝`s13_fetcher_proposal_viewer_full_nosocket.log` 04:12:15Z、`s13_app_creation_fact_green.log`）が上限で、この単位の Astra 独立実行は未完である。未完は 実 supersede 事実 `artifact.superseded` の durable 意図と再起動 drain への接続、`/supersede` HTTP route（現行は tests 参照のみで route 未存在）、canonical Event の実 route 観測、Workstream 本文 owner、live 6 rows の content_hash 付与を伴う remint と実 supersede 操作の運用受入、gate `s13_artifact_supersede_content_hash` と最小実装単位は check plan の top-level `included` gate と `outcome.open` が正本でここには複製しない、cutover receipt の source 紐付け（`3e8b0bf` が未コミット差分、2026-09-21 12:21 UTC 実測の配備 rev は `351def7`）、化石 `context_pack.jsonl` の Step20 cleanup、xbookmark `ImageID`。配備版との read-only 差分監査（HEAD 側 195 commit／deployed 側 7 commit・重複 115 file）は 2026-09-21 で終了し、観測事実は check plan の `outcome.open` にのみ保持する。
6. ~~Step 14~~ 一部完了（`docs/調査/20260907_ID統一Step14_EvidenceMemory_check_plan.json` を `post-deploy-route-verified` / `partial` に更新）。残件: cutover receipt の source が `8a867f1` のまま（Step13 と同種の source 紐付け未完了）、`go test ./...` の `Tmp/test-runtime/gomodcache` 構造化失敗（239 packages は ok）で resilience auto-repair が incident 時に必ず failed、`CountComplexityIdentityOrphans` の運用 check 未登録（`config/checks/core.json`）、loopback viewer route の認証 middleware 未確認。
7. Step 15 以降 → Step 20 → 全監査。

## 関連資料

- `docs/調査/20260916_042228_ID統一Step12_RequestResponse_check_plan.json`
- `docs/調査/20260916_073700_ID統一Step12_TTS_E2E切替調査.md`
- `docs/調査/20260916_084323_ID統一Step12_中間報告.md`
- `docs/調査/20260916_091600_ID統一Step12_listen復旧とTTS_echo.md`
- `docs/architecture/identity/IDENTITY_CANONICAL.md` Step 12 節
