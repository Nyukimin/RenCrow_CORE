# 実装状況・ロードマップ

最終コード・test 照合日: 2026-08-01

調査対象: current `main`。deploymentの稼働状態ではなく、repositoryにある実装、
production wiring、Config schema、testを基準に判定します。

ここでは、実装済み、採用済みだが未実装、deployment依存を分けます。コードが存在してもproduction wiringがないものは実装済みに数えません。

## 実装済み

| 領域 | 状態 | 現在の範囲 |
| --- | --- | --- |
| Runtime / configuration | 実装済み | config validation、composition root、HTTP server、feature registry |
| Channel / Viewer input | 実装済み | Viewer/channel から MessageOrchestrator への入力 |
| LINE Webhook / push通知 | 実装済み | `/webhook/line`、署名検証、replyとpushの分離、既存channel adapterを使う`channels send`、設定または最初の許可済み個人DMによる通知先解決、ASSISTANT専用localhost push契約と永続transport receipt |
| Agent routing | 実装済み | Mio `DecideAction`、command 優先、AgentからChat／ChatWorker／Worker／Wild／Heavyへの3層mapping。RoleはAgent identityではない |
| Kuro / Heavy Codex Exec | 実装済み | `Kuro -> Heavy provider -> kuro alias -> kuro_codex -> codex_cli -> codex exec`を`danger-full-access`固定で運用 |
| Worker / Coder | 実装済み・移行中 | proposal と side effect/policy decision の分離 |
| Viewer / events | 実装済み | REST、SSE、MonitorStore、status/jobs/ops |
| Conversation identity | 実装済み | UUIDベースの`message_id`、root `trace_id`、Viewer受付・通常／分散／音声／IdleChat／Character Runtime event・session log・TTSへの伝播 |
| Conversation / Recall | 実装済み | history、RecallPack、visibility、budget、trace、persistence |
| Knowledge Relation | opt-in 実装済み | import hook、batch、制限付き 1-2 hop、Viewer/client API |
| Workstream / policy | 実装済み | goal、task、artifact、同期policy判定、安全 gate |
| Scheduler / Heartbeat | 実装済み・移行中 | scheduled work、status、draft-only Opportunity discovery |
| Advisor / AgentProfile | 実装済み | registry、Shiro wiring、run/adoption/policy persistence、前日UTCの日次score snapshot、read API |
| 非同期UserMemory昇格 | 実装済み | L1 Raw job、全LLM idle lease、cancel、有限retry、candidate transaction、Raw compact保護、観測API |
| LLM Role profile観測 | 実装済み | CORE request metadataとGatewayのRole profile/binding/status/log/error contract |
| Economic Objective MVP | 実装済み・移行中 | Opportunity、EconomicTask、Reflection、Policy Decision、Goal／Artifact自動生成、trace付き汎用Delivery、ExternalSend監査対応。human gateを撤去中 |
| Feature route ownership | 実装済み | Chat send、Core health/module、Avatar Character Runtimeを含むproduct HTTP route登録を`internal/features/*`へ集約。Character Runtime handler本体もAvatar所有 |
| To-Be Ops UI | 実装済み | Advisor、Knowledge、Economic の card と API fetch |
| External contracts | 実装済み | LLM、STT、TTS、voice、browser、web gather の契約 |
| 標準Go配布境界 | 実装済み | CORE、CMD、PORTAL、GAMES、LLM、STT、TTS、Vision、ImageのGo entrypoint。Python／Node.jsはoptional Toolまたは外部computeに限定 |
| STT Gateway単一路線 | 実装済み | ViewerはCORE同一origin `/stt`、COREはRenCrow_STT Gatewayの`/v1/audio/transcriptions`だけを参照。外部target URL、model、language、stream URLをCORE Configから除外 |
| TTS Gateway単一路線 | 実装済み | COREはRenCrow_TTS Gatewayを参照し、Gateway markerと中継audio pathを検証 |
| Image Gateway単一路線 | 実装済み | `CORE -> RenCrow_Image -> ForgeNeo / Z-Image`、text-to-image公開contract、CORE adapter、Debug Viewerのprompt入力と生成PNG表示 |
| Vision Go Gateway | 実装済み | `rencrow-vision`、既存Vision HTTP／health／error／trace契約、Wild adapter。Python serviceはdevelopment／移行用 |
| Image Go Gateway | 実装済み | `rencrow-image`、既存Image HTTP／health／error／profile契約、ForgeNeo adapter。Python Gatewayはdevelopment／移行用 |
| 映画カタログCrawler境界 | 実装済み | COREのPython直接起動を廃止し、RenCrow_Toolsのoptional Go sidecar、hash／size検証、transactional JSONL import、明示的unavailableを実装 |
| External Portal boundary | 実装済み | `RenCrow_PORTAL`のChat／IdleChat／Games分離、Agent-owned game選択・起動・同一origin Observer、allowlist proxy |
| Interaction client profile | 実装済み | PORTALの`portal-chat`／`portal-idlechat`／`portal-games`、CMD／ASSISTANTのprofile名、client/profile組、method/path capability guard、request provenance |
| IdleChat Forecast stock | 実装済み | 0件時bootstrap、Idle/Heartbeat補充、永続化、Debug Viewer snapshot |
| IdleChat News sources | 実装済み | 報道RSS、Reddit、任意のX公式Recent Search、source type保持、SNS非確定扱い |

## 採用済み・未実装または移行・検証中

CORE側のInteraction client profile統一は実装済みです。ASSISTANTの常駐runtime、
Routine scheduler、WebSocket PUSH、acknowledgement／snooze、永続化は
`RenCrow_ASSISTANT`のMVPとして引き続き同repositoryが所有します。COREへ移さず、
共通SDKも実caller間に重複が確認されるまでは先行作成しません。

通常の画像生成MVPは実装済みです。ControlNet、ADetailer、tiled 4K upscaleは
RenCrow_Image側の型付きpresetとして後続実装し、COREへbackend固有設定を追加しません。
Mio、Shiro、Midori、Kuroが文章生成工程の一部としてCodexExe経由のImageGenを
利用する場合だけ、この通常経路の例外とします。

画像・動画認識は
`CORE -> RenCrow_Vision -> Wild backend -> RenCrow_Vision -> CORE`を採用済みです。
RenCrow_Vision側の`/health`、`/v1/models`、`/v1/vision/analyze`とmedia検証、
RenCrow_Vision接続を実装済みです。CORE側もVision Config、domain port、multipart HTTP adapter、
health集約、local／distributed orchestrator wiring、`vision.request.*` event、size上限を
実装済みです。画像・動画は先にRenCrow_Visionへ送り、正規化されたtextと必要最小限のmetadata
だけを後段Agentへ渡します。Vision失敗時はerror codeを保ったまま観測します。
RenCrow_Visionのsource defaultとsample configも`provider_model=Wild`へ統一済みです。
実認識backendを使うdeploymentではRenCrow_Vision側でbackend接続を設定し、
CORE、Vision、Wildを起動したlive E2Eをdeploymentごとに確認します。

## Kuro / Heavy Codex Execの確定済みside effect境界

KuroをMio、Shiro、Midoriと同格のAgentとし、Persona、Memory、会話SessionをCOREに
保持する設計を採用しました。HeavyはKuroと推論Targetの間に置くExecution Roleとして
維持します。CORE `main`のproduction wiringは、Kuroの通常応答を
`Heavy provider -> RenCrow_LLM kuro alias -> kuro_codex -> codex_cli`へ接続し、
`codex exec --json --sandbox danger-full-access --cd <CORE workspace> --ephemeral`を実行します。
このproduction wiringとtestを実装済み判定の根拠とし、日時付き実requestやdeploymentの
稼働証跡は`docs/調査/`へ分離します。この経路自体は未実装課題ではありません。

通常Kuro推論は`danger-full-access`を正式採用します。RenCrow_LLMはKuro／Heavy bindingの
Role profileを`agent_runtime`かつ`danger-full-access`に固定し、target sandboxにも
`danger-full-access`を要求します。`none`／`read-only`への設定変更はvalidation errorです。
COREの`codex.run` Toolは別経路で、引き続き`danger-full-access`を拒否し、
`read-only`または`workspace-write`だけを受理します。
RenCrow LLM Runtime導入やCodex thread継続は推論側の変更であり、KuroのAgent identity、
HeavyのRole identity、COREのMemory、Tool実行policyを移動しません。

## Deployment またはライブ検証に依存するもの

外部 LLM/STT/TTS/Vision/Image/GAMES runtime の起動、各演算backend、実ブラウザの
responsive表示、実音声再生は環境依存です。コード実装済みでも、各deploymentではhealth、
実request、実表示・再生を別々に検証してください。標準installerのGo配布と、外部computeの
deployment readinessは別の受入条件として扱います。

GPU、model、port、常駐方式、ある時点の起動・停止状態は各deploymentの観測情報であり、
この文書の実装済み判定へ固定しません。必要な時点で各moduleのhealth、実request、実表示・再生を
再確認し、日時付き証跡は`docs/調査/`へ保存します。
