# 設定リファレンス

## 設定正本と配置

RenCrow全体の設定配置契約は本書を正本とします。設定値のschemaと公開exampleは各設定を
所有するrepository、machine固有の実行値はruntime host上の`~/.rencrow/config/`を正本とします。
repositoryのexampleを直接productionから読み込まず、live設定をGit管理しません。

| module | schema／exampleの正本 | live設定の正本 |
| --- | --- | --- |
| CORE | `RenCrow_CORE/config/config.yaml.example` | `~/.rencrow/config/core.yaml` |
| LLM | `RenCrow_LLM/examples/rencrow-llm.yaml.example` | `~/.rencrow/config/llm.json` |
| STT | `RenCrow_STT/examples/rencrow-stt.yaml.example` | `~/.rencrow/config/stt.json` |
| TTS | `RenCrow_TTS/examples/rencrow-tts.yaml.example` | `~/.rencrow/config/tts.json` |
| Vision | `RenCrow_Vision/configs/vision.yaml` | `~/.rencrow/config/vision.yaml` |
| Image | `RenCrow_Image/config/image_gateway.json` | `~/.rencrow/config/image.json` |
| PORTAL | `RenCrow_PORTAL/portal.example.json` | `~/.rencrow/config/portal.json` |
| TRADE learning | `RenCrow_TRADE/config/learning_plan.example.json` | `~/.rencrow/config/trade/learning-plan.json` |

Linux user serviceのunit正本は各所有repositoryの`systemd`配下に置き、install時に
`~/.config/systemd/user/`へコピーします。unitにはbinary、live設定path、起動順、再起動、
hardeningだけを置きます。backend endpoint、model、routeなどの運用値をunitの`Environment=`へ
複製しません。secretは`~/.rencrow/secrets/`または権限を限定した環境ファイル、DB、log、cache、
session、生成物は`~/.rencrow/state/`等の所有directoryへ分離し、`config/`へ置きません。

旧`~/.rencrow/config.yaml`、`~/.config/rencrow/*`、`~/.config/rencrow-image/config.json`は
移行元として読み取り可能なまま残してよいですが、現行unitから参照せず、現在値の正本として
編集しません。移行は新pathへcopy、設定validation、新unitへの切替、health／readiness確認の順で
行い、確認前に旧fileを削除しません。

## 読込方法

`rencrow` は `RENCROW_CONFIG` が設定されていればそのpathを、なければ作業directoryの `./config.yaml` を読み込みます。production unitは`RENCROW_CONFIG=%h/.rencrow/config/core.yaml`を明示します。公開テンプレートは `config/config.yaml.example` です。

```bash
cp config/config.yaml.example config.yaml
RENCROW_CONFIG=./config.yaml ./build/rencrow
```

YAML 内の `${NAME}` は同名の環境変数で展開されます。secret は YAML に直書きせず、環境変数を使用してください。

複数repositoryのmodule rootは`RENCROW_WORKSPACE_ROOT`で指定できます。未指定時は現在の
working directoryから`RenCrow_CORE`を含む親directoryを探索し、最後に`$HOME/RenCrow`または
`$HOME/Documents/GenerativeAI/RenCrow`を確認します。Ubuntu固有のpathをWindows／macOSへ流用しません。

`workspace_dir/control/`を使用する場合、`agents.yaml`、`routing.yaml`、
`handoff.yaml`、`tools.yaml`の4ファイルを一括で読み込みます。directoryが存在する場合は4ファイル全てが必要です。schema version、CORE route owner、handoff owner、Tool metadata source、自動fallback禁止を検証し、不整合時は起動しません。

このcontrol setはportableな宣言であり、endpoint、secret、Toolのenabled状態、引数schema、permissionを設定しません。それらはこのConfigとCORE runtime metadataを正本とします。

外部向け`Chat`／`IdleChat`画面は独立した`RenCrow_PORTAL`が提供します。COREはPORTAL URLのredirect設定を持たず、`/viewer`ではDebug Viewerだけを提供します。

### Repository-local test runtime

引数なし`scripts/test-local.ps1`はrepository内`Tmp/test-runtime/`だけを一時領域に使いますが、Goがpackage探索する
通常名directoryへ生成`.go`を置きません。runごとのrootは`Tmp/test-runtime/_runs/<run-id>`、`GOTMPDIR`はその配下の
`_go-build`、Go cacheは`Tmp/test-runtime/_cache/go-build`へ固定します。`TEMP`、`TMP`、`TMPDIR`はrun rootへ向け、
child processには既存environmentとmergeして渡します。先頭が`_`のdirectoryを使うことを、単なる`.gitignore`の代用にしません。

runnerは自分が作成した正確なrun ID配下だけをcleanup対象にし、repository root、`Tmp`全体、globで解決した未知pathを削除しません。
旧layoutの`Tmp/test-runtime/go-build*`等に生成`.go`を検出した場合は`TEST_RUNTIME_LAYOUT_INVALID`でpreflightを失敗させ、
`go list ./...`やtestへ進みません。受入条件は、中断後を含め`go list ./...`のpackage listに`Tmp/test-runtime`が現れないこと、
全生成物とcacheがrepository外やuser profileへ漏れないこと、Windows runnerとUbuntu CIで同じlayout invariantを検証することです。

## 予約PORTの設定正本

network listenerを所有する各moduleのConfig schemaは、安定した`task_id`、host identity、
transport、bind scope/address、port、所有supervisor、停止timeoutを決定的に解決できる
意味を持たせます。既存の`server.port`、`listen`、`base_url`等の名称はmoduleごとに
異なってよいですが、実行時に推測または自動生成しません。

予約の実値は、この文書の「設定正本と配置」表にある所有repositoryのschema／exampleと
runtime hostの`~/.rencrow/config/`を正本とします。COREに全moduleのPORT番号一覧を複製せず、
deployment validationが対象hostの有効Configを合成して予約の重複を検出します。

同じConfig revision内でPORTは固定です。`port=0`、空きPORT scan、`port+1`、random PORTを
productionとRenCrow E2Eのfallbackに使いません。PORTを変更するときは所有moduleのConfig revisionを
明示的に更新し、接続元Config、firewall、health probe、運用文書を同じdeployment変更で
整合させます。

起動時の同一Task置換、所有不明processの停止禁止、解放確認、同じPORTでのreadiness確認、
error codeは
[「予約PORTと同一Taskの置換起動契約」](04_アーキテクチャ概要.md#予約portと同一taskの置換起動契約)
を正本とします。

## Capability applyとreceipt

> 状態: 採用済み・未実装です。以下の`capability_apply.*`、
> `storage.databases.capability_apply_receipts`、Durable Store entryは実装時に満たす予約契約であり、
> 現行`DatabasePathsConfig`、runtime YAML、DB data capability catalogにはまだ存在しません。

Tool、Skill、MCPの反映要求は、COREが所有する`capability apply`設定と、認証済みrequest
scopeからだけ解決します。requestへ任意のunit名、binary path、shell command、PORT、provider、
別Toolを指定する設定fieldはありません。再起動対象は論理`task_id=rencrow-core`に固定し、
実際のsystemd user／Windows native service／macOS launchd adapterの差はCORE distributionの
supervisor内部で吸収します。WSLはこの経路に参加しません。

設定schemaは次の論理項目を持ちます。

| key | 意味 | 制約 |
| --- | --- | --- |
| `capability_apply.receipt_store_id` | receiptを所有するDurable Store ID | `core.capability_apply_receipts`に固定。任意storeへの変更不可 |
| `capability_apply.response_grace_period` | 現在のrequest応答を返してからsupervisorへ移すまでの有限時間 | 正の有限duration。無限待機不可 |
| `capability_apply.verification_timeout` | 新instanceとSnapshotを検証する全体timeout | 正の有限duration。期限超過は`CAPABILITY_READINESS_TIMEOUT`または`CAPABILITY_SNAPSHOT_MISMATCH` |
| `storage.databases.capability_apply_receipts` | receipt storeのruntime物理path | owner moduleのDurable Store subtree内の絶対path。OSごとにConfigで解決 |
| `durable_store.manifest_path` | Durable Store Manifestの正本 | receipt storeのowner、writer、backup、restore、retention、fail-closedを検証 |

`capability_apply.response_grace_period`と`verification_timeout`に暗黙の無制限defaultを置かず、
deployment policyの有限値を必須とします。Configへsystemd unit、Windows service名、launchd label、
実行file、shellを複製しません。service managerは既存の所有module設定と
[予約PORT置換起動契約](04_アーキテクチャ概要.md#予約portと同一taskの置換起動契約)から同一Taskを
決定的に解決します。

`core.capability_apply_receipts`はCOREが登録するDurable Storeです。独立DBを必ず新設する意味では
なく、owner、authoritative writer、transaction、retention、backup／restore単位が既存CORE storeと
同じ場合は既存storeのschema拡張で実装できます。どの物理形態でも、次の論理recordをatomicに
更新できなければなりません。

| receipt field | 内容 |
| --- | --- |
| `request_id` | apply requestの不透明な一意ID。再接続後のstatus照会キー |
| `idempotency_key` | 同一要求の重複適用を束ねるkey。異なるpayloadとの再利用はconflict |
| `requester`／`authenticated_scope` | 認証済みuserまたはCORE Agentとscope。Model／providerはactorにしない |
| `capability_expectations` | kind、canonical name、source revision、source hashの配列 |
| `current_snapshot` | request受信時のrevision／hash |
| `expected_final_snapshot` | preflight後に固定したrevision／hash |
| `decision`／`phase` | `execute | rejected | blocked`と、accepted時の機械phase |
| `task`／`instance` | 固定`rencrow-core`、新旧instance、ReservedPortの観測値 |
| `timestamps`／`result` | 各phase時刻、完了結果、error code、検証要約 |

receiptはsecret、credential、raw tool引数、Skill本文、MCP payload、ユーザー本文を保存しません。
状態更新はatomicに行い、各decision／phase遷移はappend-only監査へ同じ`request_id`、
`trace_id`、snapshot revision／hashとともに記録します。receipt storeはcacheや人待ちqueueではなく、
CORE再起動後も残るdurable stateです。物理path、schema revision、retention、backup、
restore-check、failure contractはこの設定と`config/durable-stores.json`のManifestを照合し、未配置、
read-only、容量不足、backup範囲外では暗黙fallbackせず機能を`blocked`にします。

Manifestには少なくとも次の論理entryを登録します。`production_root_template`の実値はhost Configで
解決し、Manifestへ秘密やhost固有のabsolute pathを埋め込みません。

```json
{
  "store_id": "core.capability_apply_receipts",
  "owner_module": "RenCrow_CORE",
  "store_kind": "sqlite|append_log",
  "durability_class": "durable",
  "canonical_config_keys": ["storage.databases.capability_apply_receipts"],
  "authoritative_writer": "core-capability-apply",
  "fallback_policy": "fail_closed"
}
```

このentryのschema revision、retention、backup profile、restore check、RPO／RTOはCOREの既存
Durable Store Manifest規則に従います。`receipt_store_id`を別moduleのstoreへ差し替える構成や、
runtime home、repository、system temp、backup媒体へのfallbackは受理しません。

### Capability supervisorのOS境界

| OS | supervisor adapter | 共通の論理契約 |
| --- | --- | --- |
| Linux | systemd user | `rencrow-core`、同じReservedPort、停止確認、起動、liveness、readiness、Snapshot検証 |
| Windows | native service／supervisor | Linuxと同じrequest／receipt／phase／error意味。WSL不可 |
| macOS | launchd adapter | Linuxと同じrequest／receipt／phase／error意味。別PORT退避不可 |

supervisorは現在のCORE応答後にreceiptの`restart_scheduled`を確定し、CORE listenerとは別の
native Go worker／subcommandとして停止・起動・検証を継続します。終了したCORE processが自分の
再起動成功を証明することはできません。CLIの`status`またはPublic APIのstatus requestは、receiptを
読み、新しいinstanceから観測した結果を返します。詳細なrequest／response、状態機械、安定error codeは
[Public API仕様](06_Public_API仕様.md#capability-applyとstatus)と
[安全・自動実行・データ方針](07_安全・自動実行・データ方針.md#capability-applyの同期policy)に従います。

## 主要セクション

| section | 用途 |
| --- | --- |
| `server` | bind host、port、TLS |
| `storage`, `backup` | DB物理配置の正本、backup保存先と世代数 |
| `llm_gateway` | production inferenceのRenCrow_LLM Gateway接続。物理LLM情報は置かない |
| `mio` | Mio の会話生成 sampling と input_audio prompt |
| `runtime_topology` | module と role の配置・参照 |
| `session`, `conversation` | session、Recall、永続化接続 |
| `worker`, `coder1`-`coder4` | 実行 policy と Coder slot |
| `line`, `telegram`, `discord`, `slack` | channel adapter |
| `tts`, `stt`, `audio_router`, `vtuber` | 音声と表示連携。STT/TTSは各Gatewayの接続だけを置く |
| `vision` | RenCrow_Vision接続、timeout、CORE受付上限。Wildや物理modelの設定は置かない |
| `image` | RenCrow_Image接続とtimeout。ForgeNeo、model、生成parameterは置かない |
| `advisor`, `knowledge_relation` | Advisor 記録と Knowledge Relation |
| `heartbeat`, `workstream`, `revenue`, `economic_objective` | 継続作業と Economic workflow |
| `security`, `sandbox`, `tool_harness` | 実行制限、監査、promotion gate |
| `web_gather`, `webwright_fetch`, `browser_actor` | 外部情報取得と browser sidecar |
| `codex` | Codex CLI Tool／Advisor。Kuroの通常LLMとは独立 |
| `idle_chat.news_sources` | `IdleChatTopicSeed`と`DailyNewsBrief`の共通ニュース収集元（互換性のため設定名は維持） |
| `viewer_log`, `log` | Viewer operation log と process log |
| `games` | RenCrow_GAMES 連携。`games.auto_play` でペルソナの自発プレイ（enabled / personas / max_sessions_per_day。ペースは LLM が next_check_minutes で自己決定） |
| `trade` | RenCrow_TRADEのstatus、純粋Policy評価、非更新Risk Preview private API接続。token値ではなくowner-only token file pathを置く |

標準Go配布では、CORE Configは各Go Gatewayのbase URLとCORE側timeout／受付制限だけを
持ちます。Python／Node.js runtimeを自動導入する設定は持ちません。Browser Actor、Webwright、
`rencrow-data`はoptionalであり、明示的に有効化したhostだけが必要なruntimeを用意します。
映画カタログCrawlerは`RENCROW_MOVIE_CATALOG_CRAWLER_URL`でoptional Go sidecarへ接続し、
未設定時はbackfillをskipし、手動fetchには`MOVIE_CATALOG_CRAWLER_UNAVAILABLE`を返します。

`games.auto_play.personas`を省略または空にした場合の既定ロースターは
`mio`、`shiro`、`kuro`、`midori`の4人です。明示した場合は、その一覧だけを
自発プレイ候補として使います。`enabled`の既定値は`false`、
`max_sessions_per_day`が0以下の場合の既定値は8です。

Coderは4体セットで、identityは`coder1.name=aka`、`coder2.name=ao`、
`coder3.name=kin`、`coder4.name=gin`に固定します。欠落時はこの値を補完し、
異なる明示値はslotとidentityの不整合として起動時に検出します。COREには各slotの
有効状態、persona、tone、LightMemoryだけを置き、実行targetはRenCrow_LLMで管理します。

## 最小の考え方

`server.port` は 1-65535、`session.storage_dir` は必須です。LLMは`llm_gateway`へRenCrow_LLMの接続先とtimeoutを設定します。任意機能は `enabled: true` にしたときだけ、その機能固有の path や endpoint が必須になります。

endpoint は `http://<host>:<port>` のように環境ごとに指定してください。repository 内の例や test にある host、port、path を production 値としてそのまま利用しないでください。

`trade`は既定disabledです。有効化時はloopbackの絶対HTTP URL、owner-onlyな絶対token file path、
正のtimeoutを必須にします。COREが呼ぶのは`GET /v1/status`だけで、token値をYAMLや環境変数へ
直接置きません。

```yaml
trade:
  enabled: false
  base_url: "http://127.0.0.1:8766"
  auth_token_file: "/absolute/runtime/path/trade-control.token"
  timeout_ms: 3000
```

`llm_gateway`にはRenCrow LLM Gatewayの接続情報だけを置きます。AgentからExecution Roleへの
割当はCOREのroute contract、Role profile revisionとRoleからRuntimeへの割当、
RuntimeからBackend／Modelへの割当はRenCrow_LLMのdeployment設定です。CORE Configや
client Configへ物理Model、provider、Backend URL、Runtime、port、execution aliasの
選択設定を複製しません。

CORE Configの`image`はRenCrow_Image Gatewayの接続情報だけを持ちます。
workflow、model、LoRA、生成parameterはRenCrow_Imageが所有します。
通常の画像生成経路は`CORE -> RenCrow_Image -> ForgeNeo / Z-Image`です。
Mio、Shiro、Midori、Kuroが文章生成工程の一部として利用するCodexExe経由の
ImageGenも、COREの画像生成設定とは分離します。

```yaml
image:
  enabled: true
  base_url: "http://127.0.0.1:8780"
  timeout_ms: 600000
```

`image.enabled=true`では`base_url`と正の`timeout_ms`を必須とします。
COREは`POST /v1/images/generations`と`GET /v1/images/{image_id}.png`を使い、
RenCrow_Imageの結果をViewerへ中継します。

音声認識では、CORE ConfigにRenCrow_STT Gatewayのbase URLとCORE側の制御だけを置きます。
認識engine、model、language、外部target URL、WebSocket stream URLはRenCrow_STT側が
所有します。ViewerはCORE同一originの`/stt`を使うため、外部stream URLを設定しません。

```yaml
stt:
  enabled: true
  gateway_base_url: "http://127.0.0.1:8766"
  timeout_ms: 120000
  busy_policy: "queue_latest"
  endpoint_path: "/stt"
  debug:
    save_audio: false
    save_transcript: true
```

`stt.enabled=true`では`gateway_base_url`を絶対HTTP URL、`timeout_ms`を正数にします。
COREはRenCrow_STTの`POST /v1/audio/transcriptions`だけを呼びます。

画像・動画認識では、CORE ConfigにRenCrow_Visionの公開endpointとCORE側の受付上限だけを
置きます。Wildのendpoint、model、provider、frame sampling方式はRenCrow_Visionが所有し、
CORE Configへ複製しません。

```yaml
vision:
  enabled: true
  base_url: "http://127.0.0.1:8770"
  timeout_ms: 120000
  max_image_bytes: 20971520
  max_video_bytes: 104857600
  max_video_duration_ms: 60000
  max_frames: 8
```

`vision.enabled=true`では`base_url`と正の`timeout_ms`を必須とします。COREは起動時または
status更新時に`GET /health`を確認し、`ok=true`、`status=ready`、
`ready.model_loaded=true`を満たす場合だけreadyと判定します。HTTP 200だけでreadyにしません。
認識backend、model、media前処理の設定はRenCrow_Visionが所有します。

## LINE Webhookと通知先

LINEのcredentialはYAMLへ直書きせず、環境変数参照にします。現行fieldは`line.channel_secret`と`line.access_token`です。Access Tokenの環境変数名はdeploymentが選べますが、Configに記載した参照名とEnvironmentFileのkeyを一致させます。

```yaml
line:
  channel_secret: "${LINE_CHANNEL_SECRET}"
  access_token: "${LINE_CHANNEL_TOKEN}"

heartbeat:
  enabled: true
  interval: 30
  channel: "line"
  chat_id: "${RENCROW_HEARTBEAT_CHAT_ID}"
  x_bookmarks:
    enabled: false
    interval_minutes: 360
    timeout_minutes: 90
    run_on_start: true
    command: "rencrow-x-bookmarks"
    output_root: "/home/<user>/.local/share/rencrow/x_bookmarks/heartbeat_exports"
    max_scrolls: 100
```

`heartbeat.chat_id`が具体値なら通知とHeartbeatはその値を使用します。LINEでは33文字の`U...`を個人、`C...`をgroup、`R...`をroomとして検証します。空値または未展開の`${...}`をMessaging APIへ送ってはいけません。

LINEの通知先が空または未展開で、`heartbeat.channel`が空または`line`の場合、COREは`workspace_dir/state/line_notification_target`をfallbackとして読みます。このfileは署名検証とchannel policyを通過した最初の個人DMから作成し、modeは`0600`です。本文が`通知先登録`のDMは登録専用として通常CHATとLINE replyを開始しません。後続DM、group、roomでは自動上書きしません。明示的な`heartbeat.chat_id`は常にこのfallbackより優先します。

設定確認と送信前確認には次を使います。出力されるtargetは先頭4文字と末尾4文字以外をmaskします。

```bash
rencrow channels list --json
rencrow channels probe --json
rencrow channels send --message '【RenCrow】通知確認' --dry-run --json
```

`channels probe`と`--dry-run`は実送信しません。実送信は`--dry-run`を外した1回のcommandだけで行い、自動retryしません。

`heartbeat.x_bookmarks`はX Bookmarkのoptional browser収集です。`enabled=true`にする場合は
Heartbeat本体も有効にし、`command`が専用ログインprofileを読める
`rencrow-x-bookmarks`を指すこと、`output_root`がrepository外の絶対pathであることを必須とします。
`interval_minutes`は60〜10080分、`timeout_minutes`は1〜180分、`max_scrolls`は0〜1000です。
`run_on_start`は未指定ならtrueで、falseでは最初の収集をinterval経過後まで延期します。
profile、Cookie、token、CDP URLはCOREの設定やlogへ記録しません。

## DB物理配置とbackup

### 全module共通の物理契約

Ubuntu productionの基準配置は次の二媒体です。これはRenCrowのDB、記憶、取得原文、Ledger、
Replay、保持対象の生成物を含むDurable Data Storeの配置契約であり、Git repositoryの配置では
ありません。

| role | partition／filesystem | 論理名 | mount | lifecycle |
| --- | --- | --- | --- | --- |
| live data | GPT → LUKS2 → Ext4 | PARTLABEL `rencrow-db-crypt`、LUKS label `RENCROW_DB_CRYPT`、mapper `rencrow-db`、Ext4 label `RENCROW_DB` | `/srv/rencrow/db` | production中は常時mount |
| backup | GPT → LUKS2 → Ext4 | PARTLABEL `rencrow-backup-crypt`、LUKS label `RENCROW_BACKUP_CRYPT`、mapper `rencrow-backup`、Ext4 label `RENCROW_BACKUP` | `/srv/rencrow/backup` | backup windowだけmount |

hardware serial、`/dev/sdX`、UUIDはhost provisioning情報であり、repositoryの製品仕様へ固定しません。
service managerはlabelまたはmapperからmountを確定し、Ubuntuのsystemd deploymentでは少なくとも
`RequiresMountsFor=/srv/rencrow/db`相当の依存を持たせます。live mountがない場合は起動を失敗させ、
repository、`~/.rencrow`、OS一時directory、backup媒体へ暗黙fallbackしてはいけません。

Windows／macOSはLUKS2／Ext4を必須とせず、各moduleの設定に同じ論理subtreeを表す絶対pathを
指定します。module-facing API、所有権、fail-closed、backup整合性はOS間で同一です。

```text
/srv/rencrow/db/
  core/                  # CORE DB、Memory、Knowledge、CORE所有artifact
  trade/                 # Source、Dataset、Learning Run、Portfolio、Shadow／Ledger
  image/                 # RenCrow_Imageが保持する生成画像とmanifest
  games/                 # RenCrow_GAMESのReplayとworld／session export
  tools/                 # Toolのstaging、cache、未import artifact

/srv/rencrow/backup/
  snapshots/
    core/
    trade/
    image/
    games/
    tools/
  knowledge/
    current/
    versions/
  recovery/              # Git外の暗号化Migration Artifact等
  manifests/
```

各moduleは自分のsubtreeだけを直接read／writeし、cross-module参照はAPIまたはversioned artifact
contractを使います。Toolsが収集した映画原文をCOREへimportした後のcatalog
正本はCORE、TRADEが収集した市場原文と学習結果はTRADE、生成画像はImage、ReplayはGAMESが所有します。
RenCrow_Workspaceはsnapshot packagingを所有しますが、live data rootやbackup DBの所有者ではありません。

### Durable Store追加ガバナンス

#### 判断順序

新しいdata要件は、次の順序で分類します。前の選択肢で要件を満たす場合、後ろの選択肢へ進みません。

```text
1. request／process終了時に破棄可能か
   YES -> ephemeral。Durable Storeへ保存しない

2. 原本から決定的または安全に再取得できるか
   YES -> cache。正本・backup対象にしない

3. 大きなimmutable bytesか
   YES -> owner moduleのobject／artifact store + DB metadata

4. owner、writer、transaction、privacy、retention、backup、RPO／RTOが既存storeと同じか
   YES -> 既存storeのschemaを拡張する

5. 独立した所有・障害・復旧単位が必要か
   YES -> 新しいDurable Storeを提案する
```

次がすべて同じ場合は既存storeを再利用します。

- owner moduleとauthoritative writer
- transaction境界とconsistency要件
- sensitivity、利用者scope、ACL
- retention、削除、legal／license条件
- backup／restore単位とRPO／RTO
- schema migration lifecycle
- write特性、lock特性、容量成長

次のいずれかが異なる場合だけ新store候補とします。

- 別moduleが正本を所有する。
- 独立restore、独立retention、独立access controlが必要である。
- corruptionや高頻度writeのblast radiusを既存storeから分離する必要がある。
- schema migrationとrelease cadenceが独立している。
- 既存storeでは計測済みSLOを満たせず、indexやquery改善でも解消しない。

table数、data種別名、将来増える可能性、整理上の見た目だけを新storeの根拠にしません。複数moduleが
同じDBへ直接接続する構成は原則禁止し、owner moduleのAPIまたはversioned artifact contractへ分離します。

#### impact classと機械検証

| class | 変更 | 必須validator |
| --- | --- | --- |
| `S0` | 既存store内のcolumn、index、table、schema revision追加。owner、data class、retention、backup単位は不変 | schema互換、versioned migration、rollback、test |
| `S1` | owner moduleの既存subtree内へ新しいSQLite file、object store、append log等の物理storeを追加 | `S0`に加えowner／writer一意性、path、permission、capacity、backup、scratch restore |
| `S2` | 新しいDB engine、外部service、remote database、mount／root、cross-module accessを追加 | `S1`に加えCORE contract、三OS同等性、credential境界、dependency failure、network、運用SLO |

既存store内の変更でも、sensitivity、privacy、retention、削除、RPO／RTO、backup範囲を変更する場合は
`S1`として扱います。impact classを下げるために一つの物理DBへ無関係なdataを混在させてはいけません。
`S1`／`S2`も人の返答や別名referenceを要求しません。必要validatorをすべて通れば`validated`、一つでも
満たさなければ理由付き`rejected`です。Orchestratorはreject理由から既存store、artifact化、分離方法、
依存選択、復旧設計を再考し、新しい`proposal_revision`で最初から検証します。

#### Storage Proposal

Domain Ownerは実装前に、最低限次を型付きproposalとして定義します。

| field | 内容 |
| --- | --- |
| `proposal_id` | 一意な提案ID |
| `owner_module` | 正本とwriterを所有するmodule |
| `data_purpose` | 保存する事実と利用目的 |
| `alternatives` | ephemeral、cache、artifact、既存storeを採用／却下した理由 |
| `authoritative_writer` | 唯一のwrite owner。複数writerの場合はtransaction contract |
| `readers` | API／artifact越しを含む利用者 |
| `transaction_boundary` | atomicity、idempotency、concurrency要件 |
| `data_classification` | sensitivity、privacy、license、secret除外 |
| `retention_policy` | 保持、削除、archive条件 |
| `capacity_model` | 初期量、増加率、上限、監視閾値 |
| `schema_plan` | schema revision、migration、互換性、rollback |
| `storage_plan` | kind、Config key、module subtree、permission |
| `recovery_plan` | backup、restore-check、RPO、RTO、破損時挙動 |
| `failure_contract` | mount不在、容量不足、migration失敗時のfail-closed |
| `observability` | health、容量、integrity、migration、backup状態 |
| `change_class` | `S0 | S1 | S2` |
| `proposal_revision` | 再考ごとに増えるproposal revision |
| `parent_attempt_id` | rejectされた直前attempt。初回は空 |
| `changed_dimensions` | 前提、証拠、設計、依存、復旧等の何を変えたか |

採用前の比較資料やADRは判断証拠として保持できますが、第二の正本にしません。採用結果はCORE正本、
owner module仕様、Durable Store Manifestへ反映します。

#### Durable Store Manifest

機械可読な正本projectionとして、各moduleは標準path`config/durable-stores.json`に自身が所有する
storeだけを宣言します。COREはManifest schemaとcross-module規則を所有し、EcoSystemは全moduleの
manifestを集約検証します。host固有の絶対pathやcredentialはmanifestへ書かず、runtime Configで解決します。

```json
{
  "contract_version": "rencrow-durable-stores/v1",
  "module_id": "RenCrow_EXAMPLE",
  "stores": [
    {
      "store_id": "example.primary",
      "owner_module": "RenCrow_EXAMPLE",
      "store_kind": "sqlite|object|append_log|parquet|external",
      "durability_class": "durable|reconstructible",
      "data_classes": ["example"],
      "canonical_config_keys": ["example.storage.path"],
      "production_root_template": "/srv/rencrow/db/example",
      "authoritative_writer": "example-service",
      "readers": [],
      "schema_revision": "example/v1",
      "migration_owner": "RenCrow_EXAMPLE",
      "retention_policy": "example-retention/v1",
      "backup_profile": "example-backup/v1",
      "restore_check": "example-restore-check/v1",
      "rpo": "PT6H",
      "rto": "PT1H",
      "sensitivity": "private",
      "fallback_policy": "fail_closed",
      "change_class": "S1",
      "proposal_revision": 2,
      "parent_attempt_id": "store-plan-attempt-1",
      "changed_dimensions": ["recovery_plan", "storage_plan"],
      "lifecycle_status": "validated"
    }
  ]
}
```

`durability_class=reconstructible`では再構築元と再構築検査を別fieldで必須化します。上記JSONは
contract shapeの例であり、実際のManifest schema、validator、各module manifestは実装完了まで
存在を仮定しません。

#### lifecycleと機械gate

store lifecycleは次に限定します。

```text
proposed -> validated -> implemented -> provisioned -> active
proposed -> rejected -> revised -> proposed
active -> deprecated -> archived -> removed
```

- `proposed`はproduction Config、mount、DB fileを作りません。
- `validated`は全validatorを通過した状態であり、稼働済みを意味しません。
- `rejected`は同じproposalを再送せず、理由と変更点を持つ新revisionへ進めます。
- `implemented`はschema、migration、backup、restore-check、testが揃った状態です。
- `provisioned`は対象hostへ配置済みですが、writerを接続する前の状態です。
- `active`への昇格はmigration、integrity、backup dry-run、scratch restore、healthを通過した場合だけ許可します。
- `removed`はretention、archive、参照消滅、rollback期限を確認した後だけ許可します。

CIとdeployment preflightは少なくとも次を決定論的に検査します。

- `store_id`、Config key、owner、authoritative writerが一意である。
- production rootがowner module subtree内にあり、他storeと不正に重ならない。
- durable storeにschema revision、migration、backup、restore-check、retention、RPO／RTOがある。
- `S1`／`S2`の必須validator evidenceが揃い、`validated`以前のproposalをactiveにできない。
- cross-module direct read／write、repository／一時directory／backup媒体へのfallbackがない。
- secret、credential、host固有実値がmanifestへ含まれない。
- schema変更時に旧version読込または明示migration拒否のcontract testがある。
- backup対象とConfig上の全durable pathが一致し、scratch restoreでintegrityを検査できる。

### COREのDBと記憶

COREのDBファイルの物理保存先は`storage.databases`を正本とします。ConversationのL1とarchiveも
`conversation_l1`、`conversation_archive`だけで指定します。Ubuntu productionでは全pathを
`/srv/rencrow/db/core`配下へ置きます。

```yaml
storage:
  memory:
    session_dir: "/srv/rencrow/db/core/memory/sessions"
    operation_memory_dir: "/srv/rencrow/db/core/memory/operation"
    cold_export_dir: "/srv/rencrow/db/core/exports/parquet"
  databases:
    conversation_l1: "/srv/rencrow/db/core/databases/conversation/l1_memory.db"
    conversation_archive: "/srv/rencrow/db/core/databases/conversation/memory_archive.db"
    tool_registry: "" # SQLite ToolRegistryを使う場合だけ設定
    glossary: "/srv/rencrow/db/core/databases/knowledge/glossary.db"
    movie_catalog: "/srv/rencrow/db/core/databases/movie_catalog/eiga_catalog.sqlite"
    hobby_graph: "/srv/rencrow/db/core/databases/hobby_graph/hobby_graph.sqlite"
    investment: "/srv/rencrow/db/core/databases/investment/rencrow.db"
    advisor: "/srv/rencrow/db/core/databases/ops/advisor.db"
    sandbox: "/srv/rencrow/db/core/databases/ops/sandbox.db"
    dci: "/srv/rencrow/db/core/databases/ops/dci.db"
    skill_governance: "/srv/rencrow/db/core/databases/ops/skill_governance.db"
    workstream: "/srv/rencrow/db/core/databases/ops/workstream.db"
    revenue: "/srv/rencrow/db/core/databases/ops/revenue.db"
    persona_architecture: "/srv/rencrow/db/core/databases/ops/persona.db"
    browser_trace_to_api: "/srv/rencrow/db/core/databases/ops/browser_trace_to_api.db"
    complexity_hotspot: "/srv/rencrow/db/core/databases/ops/complexity_hotspot.db"
    super_agent_harness: "/srv/rencrow/db/core/databases/ops/superagent_harness.db"
    ai_workflow: "/srv/rencrow/db/core/databases/ops/ai_workflow.db"
    knowledge_memory: "/srv/rencrow/db/core/databases/ops/knowledge_memory.db"
    durable_store_workflow: "/srv/rencrow/db/core/databases/ops/durable_store_workflow.db"
    # capability_apply_receipts: "/srv/rencrow/db/core/databases/ops/capability_apply_receipts.db" # 採用済み・未実装

durable_store:
  enabled: true
  manifest_path: "config/durable-stores.json"
```

`durable_store.enabled=true`では`manifest_path`と`storage.databases.durable_store_workflow`を必須とし、
workflow DBは絶対pathだけを受け付けます。親directoryをruntimeが暗黙作成せず、配置済みのowner subtreeが
存在しない場合は起動を失敗させます。backupを構成している場合、このDBも`backup.core_source`配下でなければ
Config validationを通りません。

映画、趣味、投資Viewerと映画catalog backfillはこの設定を直接使用し、表示先と書込先を同じDBへ揃えます。

映画は人物、出演関係、取得履歴、評価を持つ既存`movie_catalog`を専用DBとして維持します。音楽、
小説、漫画、役者、ドラマなど趣味全域は、媒体ごとにDBを乱立させず、まず`hobby_graph`のitem、
relation、interaction、observation、preference signalとして表現します。別transaction境界、異なる
retention、独立した大量write、専用schemaが必要になったdomainだけを新DBへ分離します。

評価済み人物の関連作品収集でも新しい媒体別DBを追加しません。`hobby_graph`の論理schemaは少なくとも
次の意味fieldを持ちます。既存columnへ段階移行してよいですが、JSON payloadだけを全件decodeして検索する
実装にはしません。

| record | 必須field |
| --- | --- |
| person reference | `person_ref_id`、`movie_catalog_person_id`、確認済み外部ID、identity state、evidence URL |
| item | `item_id`、`category`、`item_type`、`display_name`、`name_original`、nullableな`name_ja`、`name_state`、`name_ja_source_url`、source record ID、canonical URL |
| relation | `relation_id`、`person_ref_id`またはitem endpoint、`target_item_id`、`relation_type`、source、evidence URL、validation state |
| description | `description_original`、`description_language`、`description_ja`、`description_translation_state`。固有名詞fieldとは分離する |
| collection receipt | run、人物、カテゴリ、source、取得・検証・import状態、件数、artifact hash、取得時刻、error code |

`name_state`は`official_ja | source_ja | original`だけを永続値として許可します。`name_ja`は取得元が提供した
日本語名に限り、`official_ja | source_ja`では根拠URLを必須とします。`original`では`name_ja=NULL`かつ
`display_name=name_original`です。固有名詞に`translated_ja`を保存しません。

#### 作品サマリ設定契約

作品説明の公開意味fieldは`summary_original`、`summary_language`、`summary_ja`、
`summary_translation_state`とし、永続fieldへ次のように対応付けます。

| 公開意味field | 永続field | 設定上の意味 |
| --- | --- | --- |
| `summary_original` | `description_original` | source説明の原文。source factとして非破壊保存し、タイトル・credit・レビュー等で代用しない |
| `summary_language` | `description_language` | 原文言語 |
| `summary_ja` | `description_ja` | source日本語説明、または原文説明だけを忠実に翻訳した日本語説明 |
| `summary_translation_state` | `description_translation_state` | `not_required | ready | failed | not_attempted` |

queryの公開状態`summary_state`は`source_summary | translated_summary | unavailable`に固定します。
`source_status=ready | unavailable`と`translation_status=not_required | ready | failed | not_attempted`は内部状態として分離し、
翻訳失敗時も原文を保持します。通常の日本語UIは`summary_ja`だけを表示し、外国語原文はdebug／内部scopeへ限定します。
原文説明からの翻訳であっても、作品名・人物名・団体名・賞名を新たに翻訳・生成せず、検証済み`display_name` tokenを使います。

summary取得のhard limitは、初回専門source responseへの同梱を最優先とし、欠落作品だけを正確な`source_record_id`、canonical URI、
ISBN、MBID、MADB／ジャパンサーチ／NDL IDでenrichmentすることです。名前だけの照会やfuzzy matchを禁止し、provider単位でbatch化します。
1 requestあたり最大20作品、1作品あたり最大2つのsummary enrichment source、固定pagination、response byte上限、timeout、
host rate limitを必須とし、公式ページの無制限fan-outを許可しません。後段sourceはサマリfieldだけを追加でき、identity・名称・relationを
上書きしません。

カテゴリ別のsummary source順は、映画=`movie_catalog`概要、ドラマ=`jpsearch -> official_public`、
アニメ／漫画=`mediaarts_db -> jpsearch -> ndl_bibliography`、小説=`ndl_bibliography -> jpsearch`、
音楽=`musicbrainz -> jpsearch`です。MusicBrainzのrelation metadataと受賞recordはサマリとして扱いません。各summaryはsource、
source record ID、canonical／evidence URL、取得時刻、content hash、language、rightsを持ちます。source／category TTLを使い、
summaryなしはnegative receiptとして同一sourceへの再取得を抑止しますが、別のapplicable sourceによる補充は許可します。
category responseは関係の成否と独立した`summary_coverage={ready,unavailable,total}`を返します。

歌手と楽曲は新DBを作らず、`hobby_graph`の`hobby_items`へ`category=music`、`item_type=artist | song`として保存し、
歌唱・作詞・作曲等を`hobby_relations`で結びます。歌詞は`hobby_music_lyrics`へ本文と権利metadataを分離して保存します。
`rights_status=licensed | public_domain | user_owned`かつ`license_reference`がある場合だけ`storage_mode=full_text`を許可し、
それ以外のWeb歌詞sourceは`reference_only | hash_only`としてURL、言語、source record ID、hashだけを保持します。
LyricFind等、閲覧可能でも複製許諾を確認できないsourceの本文をスクレイピング保存しません。sourceごとの取得結果は
`hobby_music_collection_receipts`へprovider、entity kind、権利状態、artifact hash、時刻、error code付きで記録します。
研究用途では`hobby_music_syntax_features`へtoken／行／語彙数、品詞・係り受け・反復・韻等の集計を
versioned feature JSONとして保存できます。ただし`non_reconstructable=1`をDB制約とし、歌詞本文や行列を復元できる
n-gram列、token列、長い引用をfeatureへ保存しません。解析時の一時本文保持もprovider契約が許す範囲に限定します。
Agentの照会は用途別に分離し、`music_catalog.lookup`が歌手・楽曲metadataと関係、
`lyrics_catalog.lookup`が権利状態・非復元Syntax・許諾済み全文を担当します。両方とも設定済み
`storage.databases.hobby_graph`をread-onlyで開き、物理path、table、column、任意SQLを引数に取りません。

summary補完workerを有効化するrevisionでは、`hobby_graph`へ次のqueue／indexを追加します。

| table／index | 必須field／key | 用途 |
| --- | --- | --- |
| `hobby_summary_jobs` | PK `(category,item_id)`、`state`、`source_cursor`、`next_attempt_at`、`lease_until`、`attempt_count`、`last_reason`、`updated_at` | 作品importと同一transactionで欠落summaryをqueue化し、中断再開する |
| `idx_hobby_summary_jobs_due` | `(state,next_attempt_at,category,item_id)` | workerがdue jobだけを最大20件claimする |
| `idx_hobby_item_summaries_expiry` | `(source_status,expires_at,category,item_id,source)` | expired positive／negative receiptの判定に使う |

jobのlease時刻、attempt回数、source cursorをmemory内だけに持ちません。workerを複数起動しても一つのjobを同時applyせず、
停止後はlease expiryから再開します。queue schemaまたはnamed indexがないdeploymentではworkerだけを`unavailable`とし、
既存lookupを全件scanで代用しません。

identity mapping revisionでは`external_ids_json`をcompatibility projectionとし、次の正規化表をquery正本にします。

| table／index | 必須field／key | 用途 |
| --- | --- | --- |
| `hobby_person_external_ids` | PK `(authority,external_id)`、`person_ref_id`、`state`、`evidence_url`、`observed_at`、`updated_at` | authority IDから一人へexact mappingする |
| `idx_hobby_person_external_ids_person` | `(person_ref_id,authority,state,external_id)` | 一人物のconfirmed IDを一度だけ読む |
| `hobby_person_identity_evidence` | `evidence_id`、`person_ref_id`、`authority`、`candidate_id`、一致／不一致field、`evidence_url`、`observed_at` | candidate／confirmed／ambiguousの根拠を非破壊保存する |
| `idx_hobby_person_identity_evidence_candidate` | `(person_ref_id,authority,candidate_id,observed_at)` | 同じ候補の再評価とreceipt生成に使う |

同じ`authority + external_id`を複数personへ割り当てるmigrationはcommitせず`ambiguous` receiptへ送ります。

query-timeの必須索引は、人物referenceの各外部ID一意索引、itemの`(category, normalized_title)`、source record ID、
relationの`(person_ref_id, relation_type, target_item_id)`と`(target_item_id, relation_type)`です。schemaまたは索引が
不足するdeploymentでは関連catalog capabilityを`unavailable`とし、scanへfallbackしません。

人物関連作品の外部収集providerは`RENCROW_PERSON_RELATED_CATALOG_PROVIDER_URL`で明示設定します。未設定時も
`person_related_catalog.lookup`は既存のindexed DBが利用できれば有効ですが、Worker専用
`person_related_catalog.collect`だけを未登録にします。COREは固定path
`POST /v1/person-related-catalog/collections`へ人物ID、氏名、公開人物URL、カテゴリだけを送り、DB pathやassessment値を
送りません。返却artifactは同一originからだけ取得し、64MiB上限、SHA-256、byte数を検証します。
providerが返す`source`はCORE内蔵のカテゴリ別contract-free allowlistで検証します。利用契約、利用申請、
API key、access tokenまたは課金が必要なsourceを環境変数だけで追加許可できません。
現行allowlistは`eiga.com`、`wikidata`、`jpsearch`、`ndl_bibliography`、`mediaarts_db`、
`musicbrainz`、`japan_academy_prize`、`official_public`をカテゴリ別に限定します。`ndl_bibliography`は
NDL SearchのAPI提供対象一覧で申請不要と明示されたCC BY／CC0／PDM書誌だけ、`jpsearch`は
オープンなメタデータ二次利用条件を機械的に確認できるrecordだけです。

次期source revisionでは、受賞=`wikidata_award -> official_public`、小説=`ndl_national_bibliography`を固定します。
`wikidata_award`はCC0 structured statementとreferenceだけ、`ndl_national_bibliography`は
`dpid=iss-ndl-opac-national`の国立国会図書館作成書誌だけを対象にします。後者はCC BY互換のattributionを必須とし、
NDL横断`any`、利用条件不明provider、本文、紹介文、書影を取り込みません。source registry entryはendpointだけでなく
`authority`、`rights_url`、`application_required=false`、`credential_required=false`、`attribution`、`user_agent`、
rate policy、最大page／件数を固定値として持ち、いずれかをruntime環境変数で緩和できません。

2026-08-12時点の初期rights evidenceは次に固定し、worker起動時ではなくrelease時にsource policy testで再検証します。

| source | 固定rights evidence | 採用範囲 |
| --- | --- | --- |
| Wikidata | `https://www.wikidata.org/wiki/Wikidata:Licensing`、`https://www.wikidata.org/wiki/Help:Linked_Data_Interface` | main／Property namespaceのCC0 structured statement、qualifier、reference、source提供label |
| NDLサーチ | `https://ndlsearch.ndl.go.jp/help/api`、`https://ndlsearch.ndl.go.jp/help/api/provider` | provider一覧で申請不要と示される国立国会図書館作成書誌。初期`dpid=iss-ndl-opac-national`、CC BY attribution必須 |
| ジャパンサーチ | `https://jpsearch.go.jp/static/developer/ja.html`、recordごとのrights metadata | API提供に連携機関の承諾がある利活用metadataのうち、許可licenseをrecord単位で確認できるもの |

policy testはURLへ到達できないだけで既存DB rowを削除しませんが、新規取得を`rights_unverified`にします。HTML本文や
検索結果の表示だけからlicenseを推測せず、machine-readable licenseまたは上記provider一覧の明示条件を必要とします。

production Configは自由なendpoint／TTL mapではなく、次のbounded worker controlだけを公開します。

```yaml
person_related_catalog:
  summary_worker:
    enabled: true
    interval: "5m"
    batch_size: 20
    lease: "2m"
    max_attempts: 3
  identity_mapping:
    enabled: true
    interval: "5m"
    batch_size: 20
    lease: "2m"
    max_attempts: 3
    batch_categories: 7
```

summary／identityの`batch_size`は1..20、`lease`は30秒..10分、`max_attempts`は1..3、
`batch_categories`は1..7だけを許可します。identity workerはこの間隔で人物表を走査せず、assessment遷移、identity不足、
TTL切れ、cursor付き初回migrationが作った`hobby_person_identity_jobs`だけをdue indexでclaimします。job table未配置、index不足、
provider identity endpoint不成立ではidentity収集を`unavailable`とし、名前一致や手動Config IDへfallbackしません。
source endpointは既存`RENCROW_PERSON_RELATED_CATALOG_PROVIDER_URL`の同一originに固定し、summary／identity用の任意URL設定を
追加しません。summary TTLはready=30日、semantic unavailable=7日、rights rejection=30日、identity TTLはconfirmed=90日、
ambiguous／unresolved=7日をsource policy revisionの定数として持ち、環境変数で安全境界を迂回しません。

#### Knowledge Memory indexed projection設定契約

`knowledge_memory.storage=sqlite`でindexed searchを有効にする場合、raw domain tableとは別に次のprojectionを同じDBへ置きます。

| table／index | 必須field／key | 用途 |
| --- | --- | --- |
| `knowledge_memory_search_documents` | PK `(record_type,record_id)`、`scope`、`user_id`、`title`、`summary`、`visibility`、`source_updated_at`、`indexed_at`、`content_sha256` | 公開可能fieldだけの検索projection |
| `knowledge_memory_search_terms` | PK `(scope,user_id,token,record_type,record_id)` | Go normalizerが生成した日本語2-gram／ASCII tokenの転置索引 |
| `idx_knowledge_memory_search_terms_lookup` | `(scope,user_id,token,record_type,record_id)` | exact token候補検索 |
| `knowledge_memory_index_cursor` | PK `record_type`、`last_record_id`、`indexed_count`、`state`、`updated_at` | 最大200 row単位のbackfill再開とcoverage判定 |

`personal_archive`等のprivate typeはglobal scopeへ投影しません。`user_id`を空にできるのはreview済みpublic projectionだけです。
検索入力は最大160文字、生成token最大16、候補最大50、返却最大20です。必要table／index、backfill coverage、hash整合の
いずれかが欠ける場合はdata capabilityを`blocked / indexing | schema_missing | integrity_failed`とし、payload LIKE、rowid順全件decode、
L1やWebへのfallbackを行いません。

production切替は次のpreflightを一つのpromotion receiptに記録します。`storage.databases.knowledge_memory`の
固定pathにDBを配置し、domain import manifestの`source_count | imported_count | source_hash | imported_hash`、
projection coverageの`eligible_count | indexed_count | cursor_state`、必須table／indexの一致、読み取り専用probeを検証します。
すべて成功した後だけSQLiteをactive storageにし、source JSONLはrollback証拠として非破壊保持します。
DB未配置は`unavailable/database_unavailable`、schema不足は`blocked/schema_missing`、backfill途中は
`blocked/indexing`、hash／coverage不一致は`blocked/integrity_failed`であり、別path、L1、Web、メモリ上の代替DBを使いません。

Data Capability Catalogの`knowledge_memory`は上記preflight結果を受け取り、次の投影に固定します。

| runtime状態 | catalog status | `safe_operations` | `tool_id` |
| --- | --- | --- | --- |
| DB未配置 | `unavailable/database_unavailable` | 空 | 空 |
| migration／indexing中または整合不成立 | `blocked/<reason>` | 空 | 空 |
| public projectionのみready | `available` | `public_search` | `knowledge.search` |
| public + authenticated private scope ready | `available` | `public_search`, `user_private_search` | `knowledge.search` |

publicのavailableとuser-privateのavailableは同一の権限ではありません。catalogは安全なoperationを分け、
Runtime Capability SnapshotがToolの存在を示しても、個別requestの`ToolExecutionScope`が許可しないoperationは実行しません。

#### Repository test planとPublic Docs Guard

各repositoryの引数なし`scripts/test-local.ps1`は、追跡中の全test fileを正規planの`testFiles`に包含します。
新しいGo moduleまたはProviderにtestを追加するcommitは、同じcommitで対応するworking directory、引数なしの
`go vet ./...`、`go build ./...`、Ubuntuの`go test ./...`をplan／CIへ追加します。testを減らす、patternを過剰に広げて
無関係のmoduleと混ぜる、Windowsでbehavior test binaryを実行することでguardを通しません。

CORE Public Docs Guardは`git ls-files docs`の追跡集合と`.public-docs-allowlist`を完全一致させます。
public対象の新規docを追跡するcommitは同じcommitでallowlistを更新し、非公開にすべきdocは単にallowlistへ
追加せず、配置・追跡の是非を修正します。guardの除外、skip、CIだけの条件分岐は追加しません。
Windowsの`-SelfTest`はrepo内Tmpの利用、親environmentのmerge、legacy生成Go fileのfail-closedを検証し、
Windows build／vetとUbuntu behavior testの両方が成功して初めて三OS test contractのWindows部分を完了とします。

#### 人物関連catalogの収集計画

収集対象は、人物自身の`known | like`と、映画の`seen | like`から`movie_people`の直接edgeで到達する人物の和集合です。COREのbackground Workerは30秒ごとに一人・一カテゴリだけを既存collectorへ渡し、ドラマ、受賞歴、音楽、アニメ、小説、漫画の順に永続cursorを進めます。全対象の終端後は24時間待って次cycleを開始します。外部取得の実行可否、TTL、rate limit、retry、rights判定は既存plannerとreceiptを正本とし、環境変数で緩和しません。

人物関連のsource順序、停止条件、retry対象は環境変数やprovider設定で自由に変更せず、COREの
`person-related-plan.v1`と実装のhard limitで固定します。これにより、情報量と到達可能性の高い取得先から試し、
失敗時だけ次のsourceへ進みます。requestごとのplannerは次を実行します。

- まず人物の`known | like`、または映画の`seen | like`からの直接edgeによる適格性、要求カテゴリ、既存indexed result、source・人物・カテゴリ別receiptのfreshnessを確認する。
  有効なpositive resultが十分なら外部HTTPを行わない。negative receiptは対応sourceのHTTPだけを省略し、
  別のapplicable sourceまで終了させない。
- identity解決は一run一回だけ行い、confirmed external IDを全カテゴリで再利用する。氏名だけで確定せず、
  `ambiguous | unresolved`ではカテゴリsourceを呼ばない。
- Tier 0は`movie_catalog`／`hobby_graph`のnamed indexとreceipt、Tier 1はconfirmed external IDを持つ
  カテゴリ専門sourceの直接照会とする。必要なIDがない場合だけWikidataを1回照会する。
  同一providerの複数カテゴリは一つのbounded requestへ統合する。
- 現行revisionの外部sourceは、ドラマ=`jpsearch`、受賞歴=`wikidata_award`、音楽=`musicbrainz`、
  アニメ／漫画=`mediaarts_db`、小説=`ndl_national_bibliography`に固定する。受賞はconfirmed Wikidata QID、小説はconfirmed
  NDL authority URIを必須とする。実装・fixture・live contractを持たないsourceをfallback候補としてplanへ追加しない。
- 前sourceが十分なvalidated resultを返したら停止し、`unavailable`、`ambiguous`、stale、明示coverage gapのときだけ次sourceを発行する。
  各queryは固定LIMIT／pagination上限、host別rate limit、有限timeoutを持ち、全sourceへの無条件fan-outをしない。

収集receiptには、少なくとも次の概念fieldを保存する。未実行sourceを成功や失敗に数えず、`next_source`が空ならterminalとする。

```yaml
person_related_collection:
  plan_revision: person-related-plan.v1/1
  attempts:
    - source: wikidata
      priority: 10
      reason: tier1_cross_category
      status: ready
      started_at: RFC3339
      completed_at: RFC3339
      item_count: 2
      error_code: ""
  stop_reason: enough_validated_results
  next_source: ""
```

categoryの終端状態は`complete | partial | unavailable | ambiguous | rejected`です。`attempts`にはsource hit、
provenance、rights、authority、`name_state`を対応付けます。positive receiptはsource／categoryごとのrefresh期限に従い、
結果なし・rights不足・identity未解決はnegative receiptとしてTTLを持たせます。再試行するのは429／5xx／一時的network errorだけで、
`Retry-After`と指数backoffを優先します。semantic empty、ambiguity、rights rejection、schema errorは再試行せず、次sourceへの
計画判断またはterminal状態へ進めます。

source順序を動的に昇格できるのは、source・category単位の直近90日のterminal attemptが30件以上ある場合だけです。
priorityは`validated hit率 × 平均完全性 ÷ (1 + p95 latency秒)`で、同じauthority内だけを並べ替えます。
個人評価や単発結果で順序を変えず、identity・受賞歴の低authority sourceを最終確認へ昇格しません。同率時は固定source名順で
決定し、使用統計snapshotと`plan_revision`をreceiptへ保存します。これらの計画値をprovider側の設定で上書きしてはいけません。

映画catalogの外部取得は`RENCROW_MOVIE_CATALOG_CRAWLER_URL`で設定したRenCrow_Toolsのoptional
Go sidecarだけを使用します。設定値が空またはsidecarが利用不能でも、COREはPython／Nodeの取得器、
映画.comの`/search`、別hostへfallbackしません。既存の`POST /v1/movie-catalog/crawls`と
`seed_url` requestはv1互換のまま、query取得時だけ同じrequestへ`query`を追加します。query／URLの
one-of、409候補、v2 `rencrow.movie_catalog.v2` manifest／node／edge、artifact hash／size検証と
root transactionは、Config値ではなくCOREのmovie catalog実装仕様で検証します。sidecarはCOREのDB
path、credential、個人評価を受け取りません。

保持対象の物語、音声、画像など大きな生成物はDB blobへ無条件に詰め込まず、所有moduleのsubtreeへ
immutable objectとして保存し、DBにはID、hash、media type、provenance、生成条件、作成時刻、相対参照を
記録します。現時点でImageの生成画像はImage、GAMES ReplayはGAMESが所有します。TTS GatewayはWAVを
中継するだけで永続保存を所有せず、保存が必要な音声は依頼元domainがartifactとして採用した時点で保持します。


`storage.memory`はsession JSON、運用記憶、Parquet cold exportの物理保存先です。Parquet jobも`storage.memory.cold_export_dir`を使用します。

backup保存先、世代数、外部記憶export契約は`backup`へ記載し、`scripts/rencrow-storage-backup`が`rencrow config backup-values`経由で読み込みます。backupを設定する場合は全pathを絶対pathで記載し、`storage.databases`、`storage.memory`を`backup.core_source`配下に置きます。Config上の永続情報が対象外にある場合はvalidation errorになります。

```yaml
backup:
  core_source: "/srv/rencrow/db/core"
  core_snapshot_root: "/srv/rencrow/backup/snapshots/core"
  knowledge_source: "/srv/rencrow/db/core/knowledge"
  knowledge_mirror: "/srv/rencrow/backup/knowledge/current"
  knowledge_versions: "/srv/rencrow/backup/knowledge/versions"
  recent_keep: 28
  daily_keep: 14
  weekly_keep: 8
  monthly_keep: 12
  memory:
    require_exports: true
    redis:
      enabled: true
      container: "rencrow-redis"
    qdrant:
      enabled: true
      base_url: "http://127.0.0.1:6333"
```

`redis.container`はRedisのDocker container名、`qdrant.base_url`はREST snapshot APIです。Docker volumeのhost pathは設定しません。`require_exports: true`では両backendが利用できない限りbackupを完了させません。

snapshotはCORE停止中に次の順で作成します。

1. Redisへ`SAVE`を実行し、container内の`redis-check-rdb`に通ったRDBを取得する。
2. Qdrant full snapshot APIでsnapshotを作成して取得する。
3. `core_source`と両exportを`tar.gz`へ可逆圧縮する。
4. COREを再開する。
5. `rencrow-storage-restore-check`でSHA-256、gzip、危険なarchive path、L1/L2 SQLite、Redis、Qdrantを検査する。
6. 検査済みsnapshotだけを`recent`へ確定し、03時台の実行を日次・週次・月次へ昇格する。
7. Knowledgeをmirrorし、上書き前の差分を`knowledge_versions`へ保存する。

`.env`、秘密鍵、binary、log、lock、`tmp`はCORE snapshotから除外します。映画等の永続DBは`tmp`ではなく`core_source`配下へ置いてください。圧縮archive自体は秘密情報を暗号化しないため、snapshot rootは専用mountと所有者限定permissionで保護します。

兄弟moduleはそれぞれの整合snapshotを`/srv/rencrow/backup/snapshots/<module>`へ作成します。SQLiteは
backup APIまたはwriter停止済み整合点から取得し、稼働中のDB fileだけをraw copyしません。JSONL台帳は
writeをquiesceして末尾event、hash chain、件数をmanifestへ記録します。object storeはcontent hashと
manifestを照合します。各snapshotは作成元module、schema／contract revision、作成時刻、対象範囲、
SHA-256、検証結果を持ち、scratch restoreに成功した世代だけを復旧候補とします。
Toolsの再取得可能cache、staging途中file、lock、通常logはsnapshotから除外します。生成済みartifactでも
所有moduleへimport済みで再取得可能なcopyは二重正本にせず、所有module側snapshotだけを復旧正本とします。

```bash
make test-storage-backup
make install-storage-backup
make storage-backup-check
make enable-storage-backup
make storage-backup-run-once

# 任意の保存済み世代をlive領域へ書き戻さず検査する
make storage-restore-check SNAPSHOT_DIR=/path/to/core-snapshots/recent/YYYYMMDD-HHMMSS
```

timerはJST 03:30、09:30、15:30、21:30相当の1日4回です。`RandomizedDelaySec=15m`により各開始時刻は最大15分遅延します。手動復旧では、まず上記checkerを通し、別directoryへ展開して内容を確認してからCORE停止中に書き戻します。checker自身はlive dataを書き換えません。Redis RDBとQdrant snapshotのbackend投入は、交換先を用意してから各backendの公式restore手順で行います。

`mio.generation` は Mio の通常会話と input_audio直結経路で共有する生成設定です。`stream`、`max_tokens`、`temperature`、`top_p`、`top_k`、`min_p`、`seed` を指定できます。`Chat`契約では`think=false`と`chat_template_kwargs.enable_thinking=false`を送信し、`true`は設定validation errorになります。`ChatWorker`はreasoning effortを`low`へ固定し、`Worker`はRenCrow_LLM側の設定を使います。`stream: true`ではGatewayのstreaming responseを使います。WAVは`input_audio` partとして送り、`mio.input_audio.prompt`をtext partとして同じuser messageに付与します。

`Wild`のNoThinkとstreamingは個別のlive config値ではなく、COREのrole policyとWild Agentで固定します。Gatewayへは`think=false`と`chat_template_kwargs.enable_thinking=false`を送り、Wild Agentがtoken callbackを必ず設定してstreaming requestを行います。WILD routeでは受信tokenを逐次eventへ転送します。productionのMidori AgentはRenCrow_LLM GatewayのWild roleへ割り当て、描画・文章作成などの創作依頼にroleのcontext budgetを使用します。

### RenCrow_LLM Gateway

productionでは`llm_gateway.enabled: true`にし、COREからRenCrow_LLMの単一URLへ接続します。
COREはAgentとExecution Roleを決め、Gatewayへ論理execution aliasを送ります。論理aliasは
`mio`がMio／Chat、`shiro`がShiro／ChatWorker、`worker`がShiro／Worker、
`midori`がMidori／Wild、`kuro`がKuro／Heavyを表します。alias名がAgent名と同じでも、
Agentからtargetへ直結する意味ではありません。物理Model名、Backend URL、起動port、
Codex command、RenCrow LLM RuntimeはRenCrow_LLM側で管理します。Gateway障害時にCOREから
物理targetへfallbackしません。

Kuroの通常会話はCOREでHeavy Roleへ割り当てます。Heavyのcurrent targetを
Codex subscription runtimeへ接続する設定はRenCrow_LLMの`execution_aliases.kuro`、
`roles`、`bindings` schemaで行います。KuroのPersona、Memory、会話SessionはCOREが所有し、
Codex側threadを正本にしません。通常`Kuro -> Heavy`経路は`danger-full-access`を正式契約とし、
CORE workspaceをworking directoryにしたCodex Execを使います。RenCrow_LLMはKuro／Heavyの
Role profileとtarget sandboxの両方へ`danger-full-access`を要求します。これは
COREの`codex.enabled`による限定的な`codex.run` Tool／Advisorとは独立した経路です。

外部LLM APIへの入力送信は、接続credentialや物理endpointをRenCrow_LLMが所有していても、
COREの利用者scope、privacy、実行policyを越えて許可しません。現在のGateway契約に
明示的な外部送信許可がない経路では、ローカルtargetから外部APIへ自動fallbackしません。

```yaml
llm_gateway:
  enabled: true
  base_url: "http://127.0.0.1:8090"
  api_key_env: ""
  timeout_sec: 600
```

### IdleChat episode準備とTTS先読み

IdleChatは完成済み台本を再生し、朗読時だけTTSを先読みします。採用するConfig契約は次です。
現在の実装状況は`docs/08_実装状況・ロードマップ.md`を正とします。

```yaml
idle_chat:
  episode_preparation:
    enabled: true
    generator: "codex_exe"
    ready_target: 3
    no_ready_behavior: "preparing"
    max_suffix_regenerations: 3
    codex_exe:
      sandbox: "read-only"
      ephemeral: true
  tts_prefetch:
    initial_utterances: 2
    lookahead_utterances: 3
    low_watermark_seconds: 15
    target_buffer_seconds: 30
```

`generator`は`codex_exe`だけを受理します。episode producerはCORE正本のMio／Shiro character
context、採用topic、source context、直近topic、禁止事項、構造化出力schemaを同じCodexExe
requestへ渡します。CodexExe側のprompt file、thread、workspaceをキャラクター正本にしません。
`sandbox`は`read-only`、`ephemeral`は`true`を必須とし、IdleChat台本生成からfile write、command、
外部送信、追加Toolを許可する設定は受理しません。Kuro／Heavy用`danger-full-access` profileや
汎用`codex.run`設定を流用しません。

`ready_target`は有効期限内の完成台本をbackgroundで何件まで維持するかを表し、既定値は3です。
0にはできません。`no_ready_behavior`は`preparing`だけを受理し、episode在庫がないときに
PORTALの再生request内でLLM会話生成へfallbackする設定は設けません。

`ready_target`の集計対象は制作状態が`ready`のepisodeだけです。検証NGまたは意味検査が不確実な
episodeは削除せず`needs_repair`へ保持しますが、在庫数には含めません。COREは同episodeの修復や
破棄判断を待たず、不足数を別`episode_id`としてbackgroundで追加生成します。追加生成cycleは
有限件で終了し、連続失敗時は失敗phaseと試行数を公開して次のIdle機会へ持ち越します。

`max_suffix_regenerations`は最初のNG turn以降をCodexExeで作り直す最大回数で、既定値は3です。
1以上を必須とし、上限へ達したepisodeは`failed`にします。再試行のたびに最初のNGより前の
合格済みprefixと`message_id`を保持し、NG turn以降だけを新しい`message_id`で再生成します。

`initial_utterances`は再生開始前に音声化する発話数、`lookahead_utterances`は再生中に先読みする
最大発話数です。既定値はそれぞれ2、3です。残り音声が`low_watermark_seconds`未満になる前に
先読みを開始し、`target_buffer_seconds`まで補充します。既定値は15秒、30秒で、
`target_buffer_seconds >= low_watermark_seconds > 0`を必須にします。発話数と再生秒数の両方を
満たせない短文／長文では再生秒数を優先します。これらはCORE側のqueue制御であり、物理TTSの
model、reference音声、推論parameter、GPU設定を追加しません。

episode操作CLIは`RenCrow_Tools`のnative Go command `rencrow-idlechat`へ置きます。

```text
rencrow-idlechat prepare  --core-url <url> [--count <n>]
rencrow-idlechat status   --core-url <url>
rencrow-idlechat validate --core-url <url> --episode-id <id>
rencrow-idlechat expire   --core-url <url> --episode-id <id>
```

CLIはCOREのepisode operationを呼び、stdoutへmachine-readableな結果、stderrへ運用logを出します。
COREのproduction DBやsession保存先を直接開くoption、LLM／TTS Gatewayや物理backendを指定する
optionは設けません。`status`と`validate`は最初のNG turn、NG理由、suffix再生成回数、固定prefix長、
`needs_repair`件数、補充生成状態を返します。storyではreader、listener、改変軸、genre、面白さの
方向、物語整合性errorも返します。PORTALのIdleChatは読み取り専用を維持し、これらの制作・破棄操作を
中継しません。

### 日次ニュース収集とIdleChatTopicSeed

COREはニュースの共通入力をJSTの毎日04:00に強制更新します。CORE起動時は当日cacheがなければ初期収集し、同日cacheがあれば再利用します。04:00の定期更新は同日cacheの有無にかかわらず実行します。この入力から`IdleChatTopicSeed`とユーザー向け`DailyNewsBrief`を別々に作ります。設定キーは互換性のため`idle_chat.news_sources`を維持しますが、ユーザー向けニュースをIdleChat画面の所有物にはしません。どちらも一般ニュースDBや利用者別PUSHの正本ではありません。これとは別に、同じ既定14 sourceをL1 Source Registryへ初回だけ定義・登録し、Sportsは既定disabledで収集対象外、他13 sourceはenabledとします。検証済み記事は`l1_memory.db`の`l1_news_item`へ永続化します。

起動時bootstrapはsource IDまたはURLが既存ならその行を変更しません。利用者が無効化したsourceを
再有効化せず、独自trustやintervalも保持します。新規既定行は24時間interval、`kb:news`
namespaceで登録され、enabledな13 sourceを起動直後と5分間隔のdue sweepで取得します。準備済み朝刊cacheが対象日を
満たさない場合、`DailyNewsBriefReader`は対象日の検証済みL1記事を読み取り専用fallbackとして使います。

1ジャンル／2ジャンル用のword poolは設定で無制限に増やさず、固定コア48語と当日最新語最大32語の合計80語を上限とします。当日最新語は当日`NewsAnalysisArtifact`から反映された`source_read_status=ready`の記事にある`term_notes`から、statusが`contextual`または`confirmed`の用語だけを正規化・重複排除して作ります。`NewsCollectionArtifact`だけでは最新語を作りません。固定コアと重複する語、空語、URL、24文字を超える語は除外します。RSS／Atom由来を先に採用し、`source_type=reddit|x`は合計8語までです。cacheの日付が当日でない場合は最新語を使いません。最新語抽出のための追加HTTP取得や追加LLM requestは行いません。

既定のRSS／Atom収集先は次の通りです。URLは2026-07-20にHTTP 200とXML応答を確認したものを使用します。

| category | source | URL | 1回の上限 |
| --- | --- | --- | ---: |
| `ai_frontier` | OpenAI News | `https://openai.com/news/rss.xml` | 4 |
| `ai_frontier` | Google DeepMind | `https://deepmind.google/blog/rss.xml` | 4 |
| `ai_open_source` | Hugging Face Blog | `https://huggingface.co/blog/feed.xml` | 5 |
| `ai_research` | Microsoft Research | `https://www.microsoft.com/en-us/research/feed/` | 4 |
| `ai_research` | Google Research | `https://research.google/blog/rss/` | 4 |
| `ai_infrastructure` | NVIDIA Generative AI | `https://blogs.nvidia.com/blog/category/generative-ai/feed/` | 4 |
| `ai_research` | arXiv AI Research | `https://export.arxiv.org/api/query?search_query=cat%3Acs.AI%20OR%20cat%3Acs.LG%20OR%20cat%3Acs.CL%20OR%20cat%3Acs.CV%20OR%20cat%3Acs.RO&start=0&max_results=8&sortBy=submittedDate&sortOrder=descending` | 8 |
| `general` | NHK Top | `https://www.nhk.or.jp/rss/news/cat0.xml` | 4 |
| `culture` | NHK Science/Culture | `https://www.nhk.or.jp/rss/news/cat3.xml` | 3 |
| `business` | NHK Business | `https://www.nhk.or.jp/rss/news/cat5.xml` | 3 |
| `world` | NHK World | `https://www.nhk.or.jp/rss/news/cat6.xml` | 3 |
| `sports` | NHK Sports（既定disabled） | `https://www.nhk.or.jp/rss/news/cat7.xml` | 3 |
| `tech` | ITmedia NEWS Technology | `https://rss.itmedia.co.jp/rss/2.0/news_technology.xml` | 4 |
| `business` | ITmedia Business | `https://rss.itmedia.co.jp/rss/2.0/business.xml` | 3 |

arXivは`cs.AI`、`cs.LG`、`cs.CL`、`cs.CV`、`cs.RO`を対象にし、基盤モデルだけでなく、機械学習、自然言語、画像・マルチモーダル、ロボティクスまで収集します。RSS／Atom全体は最大64件、Reddit／Xとの統合後は最大80件です。

SNS sourceは次の通りです。

| source | 既定 | 収集対象 | credential |
| --- | --- | --- | --- |
| Reddit public Atom | 有効 | `technology`、`worldnews`、`science`、`economics`のhot投稿、最大8件 | 不要 |
| X Recent Search API | 無効 | `(ニュース OR 速報 OR 話題) lang:ja -is:retweet`、最大10件 | `RENCROW_X_BEARER_TOKEN` |

利用toolと処理境界は次の通りです。

| tool／方式 | 用途 |
| --- | --- |
| Go `time.Timer` | JST 04:00の次回実行時刻を計算し、CORE停止contextで終了する |
| Go `net/http` | 10秒timeoutとRenCrow User-AgentでHTTP取得する |
| Go `encoding/xml` | RSS 2.0とAtomのtitle、link、description／summaryを解析する |
| Go `encoding/json` | Wikipedia MediaWiki Random APIとX APIの応答を解析する |
| Wikipedia Random API | Newsとは別の外部刺激シードを最大10件取得する |
| Reddit public Atom | communityの話題候補を取得する |
| X Recent Search API | 明示的に有効化した場合だけSNS話題候補を取得する |
| `rencrow-news collect` | feedと特定URLの元記事本文を取得し、immutableな`NewsCollectionArtifact`を出力する。LLMを呼ばない |
| `web_gather.fetch` | 考察phaseで、不明語検索により選んだ確認先URLの本文を直接取得・抽出する。元記事は再取得しない |
| `web_search` | 元記事本文だけでは意味を説明できない用語の確認先URL発見に限って使う。記事内容の代替取得には使わない |
| `web_gather.search` | 既定`bing_rss`で一般Web候補を検索する。credential不要。`local_cache`、SearXNG、YaCyも明示指定可能 |
| `web_gather.search_and_fetch` | 既定検索の上位URLをGo HTTPで取得・抽出する。ニュースfallbackは`bing_news_rss`と`freshness=day`を使用 |
| Worker LLM | 取得済み原文の日本語訳を作り、用語補足を確定した後にサマリとShiroの見解を日本語で作成する。HTTP取得や検索自体には使用しない。ChatWorkerはチャット画面専用 |

`idle_chat.news_sources.reddit`は既定で有効で、`communities`を1つの公開Atom feedへまとめてhot投稿を取得します。`x`は既定で無効です。有効化する場合は`bearer_token_env`でtokenを保持する環境変数名を指定し、`queries`へ公式Recent Searchのquery、category、10から100件のlimitを設定します。token本体をYAMLへ保存しません。個別sourceの失敗、429、認証エラーは当該sourceだけをdegradedとして記録し、他のRSS／Atom、Wikipedia、SNS sourceの成功結果を破棄しません。

毎日04:00 JSTのworkflowは「feed／特定URL本文の取得 → `NewsCollectionArtifact`確定」で収集phaseを終了し、そのartifactを入力としてSkill `core.build-daily-source-brief`を`Worker`で1件ずつ実行します。考察phaseは「原文翻訳 → 用語抽出 → 本文で説明できない用語だけ検索 → 検索先URL本文の直接取得 → 用語補足確定 → サマリ → Shiroの見解」を`NewsAnalysisArtifact`へ記録し、ニュース記事だけを`DailyNewsBrief`へ反映します。Wikipedia Randomは`DailyNewsBrief`へ含めず、`IdleChatTopicSeed`の外部刺激として扱います。ユーザー会話または明示Worker実行が始まった場合は日次LLM requestをcancelし、前景処理終了後に同じ収集artifact、同じ記事、短縮していない入力・contextで再試行します。この中断は失敗回数へ含めず、完了済み記事を保持します。利用者への出力順は「原文翻訳 → サマリ → Shiroの見解 → 用語補足」で、本文はすべて日本語とします。原文入力は最大12000文字、翻訳出力は最大16384 token、記事単位timeoutは10分です。元記事本文を取得できない場合は見出しやfeed summaryで補完しません。LLM未設定、timeout、形式不正、一部記事失敗ではanalysis statusを`failed`または`partial`にし、推測せず確認不能を明示します。収集失敗を考察失敗へ、考察失敗を収集失敗へ丸めません。

### ニュースworkflow CLI

採用するTools側の実行pathとcommandは次です。現在の実装状況は`docs/08_実装状況・ロードマップ.md`を正とします。

```text
RenCrow_Tools/tools/news/news_brief/cmd/rencrow-news

rencrow-news collect --output <collection.json> [--max-items <n>]
rencrow-news analyze --input <collection.json> --output <analysis.json> [--core-url <url>]
```

両subcommandはstdoutへmachine-readableな完了要約、stderrへlogを出し、artifactは指定pathへatomicに書き込みます。終了codeは`0=ready`、`2=引数またはschema不正`、`3=partial`、`4=収集失敗`、`5=考察失敗またはCORE考察port unavailable`とします。credentialや記事本文をlogへ出しません。`--core-url`はCOREの論理入口だけを指定でき、RenCrow_LLM Gateway、Runtime、物理Backend、Modelを指定するoptionは設けません。source一覧、件数上限、timeoutの既定値をToolsへ複製せず、CORE所有のpolicy／contractから受け取ります。

現在のプロセス内cacheと解決済み取得先はDebug Viewerの`情報収集`タブ、または`GET /viewer/idlechat/collection`で確認できます。応答には使用Skill、取得項目のtitle、category、source、source type、元URL、原文取得だけを表す`source_read_status`、後続工程を表す`processing_status`、表示可能な工程別理由`processing_error`、両状態の件数集計、原文翻訳、サマリ、見解、用語補足と、credentialを除いたsource設定、利用toolを含みます。収集phaseで取得できた項目は後続処理の状態にかかわらず`items`へ全件残し、`total`は常に`items`の件数と一致させます。`source_read_status=unprocessed`と`processing_status=pending`は未着手であり失敗ではありません。原文取得後の翻訳失敗は`source_read_status=ready`、`processing_status=translation_failed`として区別します。このGETは観測専用であり、収集jobを起動しません。ユーザー向けChatはこのViewer endpointを呼ばず、CORE内部の`DailyNewsBriefReader`から`DailyNewsBrief`を読み取ります。`DailyNewsBrief`が未準備、空、対象日より古い場合だけ`LiveNewsSearch`へフォールバックし、検索時刻を回答へ付けます。

CLIでは`rencrow web-gather search "query"`が標準`bing_rss`、
`--provider bing_news_rss --freshness day`がニュース検索です。Bing RSS、記事site、networkの
いずれかが利用不能なら、検索結果を捏造せず`diagnostics.error`と非0終了で返します。

`conversation`の永続化設定は次を使用します。SQLiteの物理pathは`storage.databases`へ記載します。

| key | 用途 |
| --- | --- |
| `redis_url` | active threadを保持するRedis接続先 |
| `vectordb_url` | 意味検索を行うQdrant接続先 |

会話要約と非同期ProfileExtractorはRenCrow_LLM GatewayのWorker経路を使用します。`conversation.embed_model`にはRenCrow_LLMの論理embedding aliasを指定します。空の場合はvector embeddingと意味検索だけを無効化し、Redisのactive thread、L1 SQLiteのraw message、L2 SQLite archiveは維持します。

active threadが12メッセージへ到達した場合、次のactive thread作成と最新メッセージ保存を同期完了してから、旧threadのLLM要約を45秒上限のbackground処理でL2へarchiveします。応答完了はWorker要約を待ちません。Workerが時間内に応答しない場合は、期限切れcontextを保存処理へ再利用せず、新しい5秒contextで決定論的な簡易要約をL2へ保存します。

### Profile昇格設定

次のキーは非同期UserMemory昇格の現行Config schemaです。

| key | 契約 |
| --- | --- |
| `profile_promotion_enabled` | L1 Rawからの非同期UserMemory候補抽出。既定値`true` |
| `profile_promotion_idle_grace_seconds` | 全LLM利用数が0になってから処理開始までの継続Idle時間。既定値`10` |
| `profile_promotion_timeout_seconds` | ProfileExtractor 1回の上限時間。既定値`45` |
| `profile_promotion_batch_messages` | 1回に読む未処理ユーザー発話数。既定値`24`。超過分を破棄せず次回batchへ送る |
| `profile_promotion_max_attempts` | LLM、parse、L1保存失敗の最大試行回数。既定値`5`。busy/cancelは数えない |

非同期ProfileExtractorは`llm_busy`の`active_count=0`が
`profile_promotion_idle_grace_seconds`継続した場合だけ低優先度で開始します。この判定には
IdleChatを含む全provider sourceを含めます。処理開始後に通常LLM要求が来た場合はcancelし、
busy/cancelを抽出失敗として数えず次回Idleへ延期します。`profile_promotion_batch_messages`は
1回の処理量だけを制限し、入力Rawの削除や要約による代替を許可しません。job状態と失敗理由は
`GET /viewer/memory/profile-promotions`で確認できます。

### RenCrow_TTS Gateway

production TTSは`tts.gateway_base_url`で指定したRenCrow_TTS Gatewayだけを使います。

```yaml
tts:
  enabled: true
  gateway_base_url: "http://127.0.0.1:7870"
  timeout_ms: 120000
  voice_id: "mio"
  speed: 1.2
  tls_skip_verify: false
  output_dir: "./workspace/tts"
  playback_commands: []
```

`gateway_base_url`にRenCrow_TTS Gatewayを設定します。target、model、reference音声、
seed、推論parameter、target credentialはRenCrow_TTS側で管理します。

### TTS発音日次チェック

`tts.pronunciation_check`はCORE Schedulerが所有する運用タスクです。`schedule`はUTCの5フィールドcronで、`cron 30 19 * * *`はJST 4:30です。実行前に`gpu_match`で選んだGPUを`idle_samples`回確認し、空きVRAMが`min_free_mb`以上、利用率が`max_utilization_percent`以下のときだけRenCrow_TTSのTool APIを呼びます。条件を満たさない場合は失敗にせず、`retry_interval_seconds`後へ延期します。

### News Pack Article Reader

News Packの元記事は通常HTTP取得を常に第一経路とします。Cloudflare等のbot challengeを`blocked_by_policy`と判定した場合だけ、明示有効化したJina AI Readerを限定fallbackとして使えます。既定はdisabledで、現行hostはOpenAI公式記事だけを許可します。

```yaml
web_gather:
  article_reader:
    enabled: true
    endpoint_prefix: "https://r.jina.ai/http://"
    allowed_source_hosts:
      - "openai.com"
    timeout_ms: 30000
```

`endpoint_prefix`はConfig検証済みのJina Reader HTTPS prefix、`allowed_source_hosts`はwildcardやURLではなく完全一致の公開host名だけを受け付けます。Readerの未設定、timeout、元URL不一致、本文不完全は`unavailable`とし、RSS description／contentと既存項目を削除しません。

## Secret とローカルデータ

- API key、token、private key は環境変数または外部 secret store に置く。
- `config.yaml`、runtime DB、log、cache、generated artifact は commit しない。
- 別machineへの移行では、Configはhost-bound値を再解決し、durable Stateは所有moduleの整合性ある
  export、secret実値はsource host上で暗号化したMigration Artifactとして扱う。平文Config／secretや
  稼働中DBの単純file copyをRenCrow_WorkspaceのGit snapshotへ追加しない。
- 外部 bind を行う場合は TLS、認証、network allowlist を別途設定する。
