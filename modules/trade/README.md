# Trade module contract

This package is CORE's stable DTO boundary for the read-only RenCrow_TRADE
private API. It validates only the current Disabled Foundation. It contains no
Broker, order, portfolio, or execution implementation.
